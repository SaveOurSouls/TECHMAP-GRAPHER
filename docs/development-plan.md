# Предложенный порядок разработки TECHMAP-GRAPHER

М4-72–73 завершены: общие размеры физических пайпов автоматически применяются
ко всем проходящим проводам, а поправки концов остаются индивидуальными.
Добавлены устойчивые ключи участков, серверная валидация, cross-selection,
регрессии, API smoke, браузерная проверка и ZIP. Версия `0.47.0-m4-73`.
[Протокол](m4-72-73-shared-dimensions.md). Следующий свободный индекс — М4-74.

М4-70–71 завершены: управление пайпом и исходными перегибами, видимые ручки, автоматические выходы, создание между выбранными узлами, таблица пайпов и Shift+клик для списка жил. Версия `0.46.0-m4-71`; 903 клиентских теста, TypeScript, сборки, API с перезапуском, браузер и ZIP. [Протокол](m4-70-71-pipe-editing.md). Следующий свободный индекс — М4-72.

М4-66–69 завершены: стиль референса С1, единая геометрия таблиц и заливка рисунков по цвету провода, радиусные переходы и физические узлы, документные таблицы с cross-selection. Версия `0.45.2-m4-69`; клиент 899/899, сервер 718/718, сборки, API, браузер и ZIP проверены. [Протокол М4-66–69](m4-66-69-reference-design.md). Следующий свободный индекс — М4-70.

М4-64–65 завершены: исправлен отказ запуска при сохранённых снимках компонентов без размещений. Версия `0.45.1-m4-65`; 775 серверных тестов, сборки клиента/сервера, API с повторным запуском и ZIP проверены. [Причина, проверки и обновление](m4-64-65-startup-retained-snapshots.md). Следующий свободный индекс — М4-66.

М4-60–63 завершены: спецификация с абстрактными и дополнительными позициями, каналы и защита, Т-ветвление, несколько выходов разъёма и распределение по Э4. Версия `0.45.0-m4-63`; клиент 897/897, сервер 772/772, сборки/API/браузер/ZIP проверены. [Протокол и границы реализации](m4-60-63-harness-channels.md). Следующий свободный индекс — М4-64.

М4-58–59 завершены: трассировка соседних автоматических проводов Э4, рамки рисунков, поворот разъёмов и кнопки размеров. Версия `0.44.0-m4-59`; клиент 892/892, сервер 764/764, сборки/API/браузер/ZIP проверены. [Протокол и ограничения](m4-58-59-routing-and-drawing-rotation.md). Следующий свободный индекс — М4-60.

М4-53–54 завершены: привязка рёбер рисунков по двум осям, постоянные фигуры,
правые свойства, числовой ввод до Enter/blur, стандартный цвет линии и устойчивость
локальной сессии. Отдельные наборы Э4/Чертёж/Маршрут проверены.
Версия `0.43.1-m4-54`; клиент 878/878, сервер 762/762, сборки/API/браузер/ZIP.
Код `9627c3b` отправлен в GitHub. [Протокол и ограничения](m4-53-54-drawing-editor-and-server.md).
Следующий свободный индекс — М4-55.

М4-51–52: привязка первой строки перемещаемого компонента Э4 к ближайшей строке другого,
перестановка строк экземпляра без изменения ID и данных контактов,
резка/разделка только в Чертеже, таблица соединений в Э4/Чертеже/Маршруте.
Версия `0.43.0-m4-52`. Клиент 875/875, TypeScript/build; сервер 760/760.
[Протокол, API, браузер и ZIP](m4-51-e4-row-order-and-documents.md).
На момент завершения М4-52 следующий индекс был М4-53.


М4-50: все InfoHint переведены в верхний слой браузера; исправлено обрезание
подсказок панелями и модальными окнами, добавлено размещение у краёв экрана.
Версия `0.42.1-m4-50`; 867 клиентских тестов, сборка и браузерная проверка.
[Протокол и ZIP](m4-50-hints-top-layer.md).

М4-47–49 завершены по нумерации, уточнённой пользователем:
1. М4-47 — формат и вычислитель.
2. М4-48 — интерфейс исходника/артикула и дополнения.
3. М4-49 — сквозные проверки, API, браузер и ZIP.

Версия `0.42.0-m4-49`. Подробности и проверки:
[m4-47-49-series-drawing-generator.md](m4-47-49-series-drawing-generator.md).
Изменения остаются в рабочем дереве, без коммита и отправки по правилам задачи.

Предшествующее исправление (исторический протокол М4-47): исправлены цельность рисунка Э4 и возврат из редактора без сохранения.
Проверки и пакет: [m4-47-whole-drawings.md](m4-47-whole-drawings.md).
Генератор выполнен под исходными номерами М4-47–49; см. результат выше.

М4-46: подготовлено предложение генератора рисунков серии «начало · период × N ·
конец» с независимой нумерацией, защищённой вычисляемой основой и дополнениями
артикулов. См. [m4-46-series-drawing-generator-proposal.md](m4-46-series-drawing-generator-proposal.md).

М4-45: отполирован редактор рисунков по C1–C3: текст, масштабируемые маркеры,
приоритет внутреннего буфера, привязки между фигурами и компактная верхняя лента.
Проверки и ограничение smoke — в [m4-45-drawing-editor-polish.md](m4-45-drawing-editor-polish.md).

М4-41: усилен API smoke рисунков по разделам Э4/Чертёж/Маршрут, включая полный
набор контактов, отклонение неполных рисунков, независимую очистку и перезапуск.
Протокол: [m4-41-drawing-target-smoke.md](m4-41-drawing-target-smoke.md).
Далее [М4-42 — таблица маркировок и экземпляры](m4-42-markings-card.md),
М4-43 — импорт XLSX, М4-44 — привязки и составная защита.

М4-38–М4-40: автономная приёмка завершена. Найдено и исправлено несохранение
размеров окон таблиц: ширина/высота, угловой маркер, клавиатура и Ctrl+Z.
Версия `0.40.1-m4-40`: 841 клиентский и 753 серверных теста, сборки,
API с перезапуском и браузер прошли. ZIP: `artifacts/m4-40/TECHMAP-GRAPHER-win-x64.zip`.
Протокол: [m4-38-40-autonomous-acceptance.md](m4-38-40-autonomous-acceptance.md).
Изменения локальные, без коммита и push согласно правилам, переданным в эту задачу.
Пользовательская приёмка реального жгута остаётся отдельной; для продолжения
технических проверок участия владельца не требовалось. Следующая карточка не открыта.

Ниже — история завершённых этапов.

М4-34–М4-37 завершены: окна документов на поле, контакты рисунков и общий
графический выход, три режима размеров и расчёт физической длины.
Версия `0.40.0-m4-37`. 832 клиентских и 746 серверных тестов, сборки,
API с перезапуском, браузер и ZIP без пользовательских данных проверены.
Итог, ограничения и изменённые файлы: [m4-37-drawing-verification.md](m4-37-drawing-verification.md).
Коммиты этапов отправлены в `codex/restore-20260918-0235`.
Новая карточка не начата. Ниже — история этапов.


М4-36: размерные линии и расчёт длины по общей линии или полным участкам завершены.
Протокол: [m4-36-drawing-dimensions.md](m4-36-drawing-dimensions.md).

М4-34: окна документов на поле реализованы и проверены (828 клиентских, 5 API).
Протокол: [m4-34-drawing-windows.md](m4-34-drawing-windows.md).
В работе запрос: М4-35 контакты рисунка → М4-36 размеры и длины.

M4-33 завершён: экран по всему диапазону смещённых проводов, постоянная
ориентация на горизонтальном пучке, независимость основных трасс от экрана,
обычное подключение к контакту с изгибами и удобный захват у таблицы.
Версия `0.39.0-m4-33`; 827/827 клиентских и 735/735 серверных тестов,
сборки, API с перезапуском, браузер и ZIP без пользовательских данных.
Протокол: [m4-33-screen-routing.md](m4-33-screen-routing.md).
Следующая карточка не начата. Ниже сохраняется история этапов.

M4-30–M4-32 завершены: фон штриховки, удобные маркеры малых фигур,
масштаб поля, графические кнопки, массивы с контактами и пропорциональный
масштаб размещённых рисунков Э4/чертежа. Версия `0.38.0-m4-32`.
825/825 клиентских и 735/735 серверных тестов, сборки, API с перезапуском,
браузер и ZIP без пользовательских данных проверены.
Протокол и ограничения: [m4-32-drawing-scale.md](m4-32-drawing-scale.md).
M4-30 `16a14f7`, M4-31 `a9b3029`. Следующая карточка не начата.
Ниже сохраняется история предыдущих этапов.

M4-27–M4-29 завершены: поля библиотечной таблицы, растяжение смешанного
выделения, рисунки по разделам и импорт PNG/JPG/BMP/SVG/HEIC.
Версия `0.35.0-m4-29`; клиент 821/821, полный сервер 731/731,
финальные валидаторы 19/19; сборки, API с перезапуском, браузер и чистый ZIP.
Протокол и ограничения: [m4-29-drawing-targets-import.md](m4-29-drawing-targets-import.md).
M4-27 `549dc8e`, M4-28 `3116b7f`; очередная карточка не начата.
Ниже сохраняется история предыдущих этапов.

Порученный диапазон **M4-22–M4-26 завершён** 20.09.2026.
M4-26: отдельная схема резки/разделки с общими расчётами, кабельной оболочкой,
слоями жил, Undo, ревизией и переходом на Чертёж. Версия `0.34.0-m4-26`;
817 клиентских / 729 серверных тестов, сборки, API с перезапуском, браузер и ZIP.
Протокол: [m4-26-cut-diagram.md](m4-26-cut-diagram.md).
Следующий этап не открыт. Ниже — история планирования и завершения карточек.

M4-25 завершён: таблицы соединений/спецификации на поле, стабильные связи строк,
выноски с независимыми концами и номером из BOM. Версия 0.33.0-m4-25;
810 клиентских / 729 серверных тестов, API/браузер/ZIP. Протокол m4-25-drawing-documents.md.
Следующий и последний порученный этап — M4-26: графическая резка/разделка.

M4-24 завершён: оболочки и закреплённая библиотечная защита по интервалам ветвей.
Версия 0.32.0-m4-24; 806 клиентских тестов, сервер 726/726, API/браузер/ZIP.
Протокол m4-24-branch-coverings.md. Далее M4-25 — таблицы и позиционные выноски.

M4-23 завершён: физическая топология, общие выходы, редактируемые участки,
явные маршруты и подсветка ветвей. Версия 0.31.0-m4-23; 801 клиентский / 723
серверных теста, API с перезапуском, браузер и ZIP без пользовательских данных.
Протокол: m4-23-physical-topology.md. Далее M4-24 — оболочки и защита по ветвям.

Дата актуализации: 2026-09-20. Статус: ПРИНЯТ владельцем; работа в M4.
M4-09–12 завершены по протоколам; M4-13 сохранила совокупный срез в GitHub
(`129cc35`). Завершена карточка M4-14 — снятие общей оболочки кабеля:
`m4-14-cable-sheath-strip.md`. Завершена M4-15 — редактор рисунков библиотеки и размещение по артикулам.
M4-16 завершена — копирование, рамочное выделение, общие свойства, поворот
и удаление фигур; протокол `m4-16-drawing-selection.md`.
M4-17 завершена — единый выбор артикула и предпросмотр, независимые рисунки на Э4,
видимость и прямые символические связи; протокол `m4-17-article-drawings.md`.
Основание: requirements-notes.md, requirements-audit.md, acceptance-examples.md.
Документ — план работ, НЕ итоговое ТЗ и НЕ инструкция. Владелец подтвердил
«Принимаем, стартуем». Работа идёт ограниченными карточками; весь план
не выполняется одним запуском. M0 и M1 завершены; в M2 закрыты карточки M2-01–03 и
M2-05–10. M2-08 реализует формат v3, связи контактов между видами, варианты
артикула, их предпросмотр и хранение. M2-09 добавляет read-only импорт публичной
Google Sheets. M3-01 закрепляет выбранную версию и артикул библиотечного компонента
в независимом документе жгута. Сквозной срез M3-12 проверен; выполнены M4-01–03
и восстановленный M4-03-r6. Карточка M4-04 закрыла выявленный аудитом
пробел серверной проверки послойной разделки; протокол и очередь дальнейших
карточек — `m4-04-post-restore-audit.md`. M4-05 проверяет импорт Google Sheets по книге,
независимые пины, преднастройки и размещение Э4; протокол — `m4-05-library-import-e4.md`.
M4-06 завершена — редактор серии по новому запросу; протокол `m4-06-series-editor.md`.
M4-08 завершена — отображение имени контакта из закреплённой библиотеки и короткого артикула терминала в Э4. Протокол — `m4-08-contact-labels.md`. M4-07 завершена — исправление цвета при коллизии, глобальное перестроение
трасс и прямой отвод экрана; протокол `m4-07-routing-audit.md`. Снятие оболочки
отложено. M2-11–M2-13 ждут схемы отдельных баз.

## Способ работы

Инкрементальная разработка с ранней проверкой технических рисков: сначала
малая сквозная функция, затем расширение на согласованный объём V1.
Для каждого этапа заранее определить результат и проверку. Проверять модель
данных и расчёты отдельно от изображения, затем проверять их интеграцию.

Ориентиры: ISO/IEC/IEEE 29148:2018 для инженерии требований и
ISO/IEC/IEEE 12207:2026 для процессов жизненного цикла. На дату проверки
официальная IEEE-страница отмечает редакцию 2017 как заменённую редакцией 2026.
Использованы публичные описания стандартов; полного аудита соответствия нет.
Стандарты не предписывают нам конкретный стек или длительность итерации.
Разбиение ниже — проектное предложение, а не цитата из стандарта.

Источники:
- https://www.iso.org/standard/72089.html
- https://standards.ieee.org/ieee/12207/11416/

## Архитектурное направление (решение M0-05)

Web-first модульный монолит: обычный браузерный клиент обращается только к
версионированному HTTP API. Локально тот же сервер запускается из самодостаточного
Windows ZIP и слушает loopback; позднее без переписывания клиента размещается в LAN.
Не начинать с микросервисов, WebView или облачного хостинга. Продуктовый baseline:
.NET 10 LTS, ASP.NET Core/EF Core SQLite, React/TypeScript/Vite и собственный
Canvas 2D renderer. Кандидатные версии, проверка locked restore, лицензии и бюджеты записаны в
`docs/m0-05-architecture.md`; порядок M1 — в `docs/m1-cards.md`.

Разделить:
- справочные позиции/версии и графические шаблоны;
- электрическую сеть, физические участки и визуальные проекции;
- размеры изделия и условные координаты на холсте;
- нормативный маршрут, состояния иллюстраций и фактические события производства;
- плановую потребность, выданные задания и доступные/потреблённые остатки.

Схема и чертёж — два представления одной модели, а не две базы с взаимной синхронизацией.
Команда изменения применяется к модели, выполняет проверки и обновляет представления.
Внутренние ID не зависят от XS1/W1, номера строки, имени или артикула.
Документ привязан к конкретной версии данных. Проект не читает «самую свежую»
позицию источника без согласованного обновления.

Предлагаемое хранение: транзакционная локальная БД и каталог вложений с манифестом.
Файлы приложения отделены от пользовательских данных. Импорт источников и экспорт
документов подключаются через адаптеры. Доступ к БД через слой приложения, чтобы
LAN-версия позже не требовала общего файла базы на сетевом диске.
Сохранение через команды и отдельный учёт факта должны позволить будущую серверную
проверку версий, но сетевые блокировки, роли и распределённое редактирование сейчас не делать.

## Этапы и контрольные точки

Ни один этап не считается одной задачей на один ответ. Перед началом этапа
разбить его на карточки по правилам ниже. M0 и M1 не являются рабочей V1;
вся обещанная первая версия включает M0–M9. Сроки в днях сейчас не обещаем:
после M0 оценка по задачам, после M3 — пересмотр по фактической скорости.

### Ускоренный сквозной срез после M2-03

13 сентября 2026 года порядок внутри M2/M3 изменён после пользовательской
проверки темпа. Полный редактор больше не ждёт последовательного закрытия всех
карточек M2. Работа идёт двумя независимыми потоками:

1. профили пяти уже известных таблиц реальной книги и рабочий поиск по ним;
2. общая модель жгута и полноценная компоновка редакторов Э4/Чертежа.

Редакторный поток разбит на четыре проверяемые карточки: `E-01` — общая модель и
сохранение документа жгута; `E-02` — рабочее пространство Canvas с инструментами,
свойствами, слоями и нижним каталогом; `E-03` — соединители, контакты и провода Э4;
`E-04` — те же экземпляры в Чертеже, трасса, абсолютная длина, слои и отмена.
Первый пользовательский ZIP этого потока выпускается после `E-04`, когда весь
сквозной сценарий сохраняется и восстанавливается после перезапуска.
После первого среза `E-03` расширяется по фактическим требованиям Э4: ортогональные
ломаные с редактируемыми сегментами, явные электрические узлы, два отображения
пересечений, дифференциальные пары и перемещаемые экраны. Расширение принимается по
работе Canvas в пользовательском ZIP, а не только по автоматизированным тестам.

Расширенный каталог, Google Sheets и универсальный редактор шаблонов продолжаются
после либо параллельно этому срезу. Отсутствие отдельных схем баз проводов,
соединителей и аксессуаров не блокирует редактор: до их получения используются
универсальные проектные компоненты. Нельзя объявлять эти три промышленных профиля
готовыми без фактических таблиц.

| Этап | Зависимости | Работы | Проверяемый результат / условие выхода |
|---|---|---|---|
| M0. Архитектура и риски | Подтверждение плана | Владение полями A01, обследование источников, выбор стека, эксперименты: направляющая/текстура, поиск большой базы, переносимый web-хост, self-contained ZIP и сохранение. Изолированные прототипы, не новая финальная система. | Записи решений с альтернативами, результаты экспериментов, список библиотек/лицензий, воспроизводимые замеры, карточки M1. Неудачный эксперимент ведёт к пересмотру решения. |
| M1. Данные и восстановление | M0 | Проект/жгут/версии/вложения, команды изменения, единицы, числовые расчёты, сохранение/открытие, локальный web-хост, архив приложения и проекта, миграционный каркас. | Проект с двумя жгутами после перезапуска сохраняет идентичность; отдельное восстановление не меняет оригинал; EX-01–03 проходят как расчётные тесты; self-contained ZIP запускается без установленной среды. Никаких обещаний полного редактора. |
| M2. Источники, каталог, шаблоны | M1, A01 | Снимок внешних баз, XLSX, read-only адаптер Google Sheets с разрешённым доступом, импортные ошибки, поиск/сохранённые фильтры, избранное. Редактор примитивов, точки, два вида, параметры, контактные типы, связывание шаблона. | Отсутствующая сеть не мешает работать; старый проект не меняется от импорта. Создан шаблон, связан с двумя артикулами; 2/10 контактов и сопоставление точек корректны. Приватные исходники не попадают в Git. |
| M3. Первый сквозной жгут | M1, минимум M2 | Два соединителя, провода, Э4, прямые трассы, переподключение, размеры и припуски, локальный/глобальный размер, предупреждения, Ctrl+Z, слои, простая карта резки/материалов. | Пользователь создаёт небольшой жгут от схемы до таблицы резки, сохраняет и открывает. Передвижение не меняет длину, переподключение видно в обоих представлениях. Это первая функциональная демонстрация, не урезанная замена V1. |
| M4. Полная конструкция и разделка | M3 | Многожильные кабели, переменные слои L/D, переходы сечения, пайка/сварка/обжим, экраны, скрутка, гофра/оплётка/ТУТ/стяжки. Повторяемые текстуры, отключаемые привязки, маркировки/XLSX, составные трубки, графические примитивы и изображения в полном согласованном объёме. | Трёхслойная разделка соответствует эталону; отсутствующие слои и незавершённые длины обработаны правильно. Усик учтён один раз, вспомогательный вид не дублирует сырьё. Автоохват и подбор артикула выключаемы. |
| M5. Полуфабрикаты и маршруты | M4 | AON-блоки полуфабрикатов, операции q/K, состав, зависимости и наследование состояний, несколько родителей, сохранение шаблона и маршрута. Редактор состояния этапа без изменения конструктора. | Две независимые цепочки полуфабрикатов сходятся в сборку; состояния не теряют идентичность и не удваивают материалы. Цикл зависимостей обнаружен; конфликт свойства показан. |
| M6. Время и оборудование | M5, A02 | Производительность и эффективные дни, целые изделия на рабочих местах, передаточные партии, неполный остаток, подготовка, очередь станков по инвентарному номеру. Технологический путь и ресурсная длительность отдельно. | EX-02/03 и дополнительные ручные эталоны: развилка/схождение, два места, дефицит станка, передача 10+остаток, невозможный цикл. Нормативы не меняются от агрегирования заданий. |
| M7. Производственные документы | M5; M6 для времени | Вёрстка этапов и фото/аннотаций, карты резки/обжима, перечень материалов, Э4, таблица прозвонки. XLSX/PDF/PNG, многостраничность, PDF-комплект. | Данные отчётов совпадают с моделью и согласованными формами, единицы и страницы не обрезаны. Получение форм владельца обязательно для приёмки окончательного оформления; временная форма обозначена демонстрационной. |
| M8. Выпуск, задания и контроль | M6, M7, A03 | Агрегация идентичных позиций между активными проектами, таймеры, распределение, факт/брак/доработка, остатки, корректировки, версии заданий. Серийные номера, повторные попытки и выборочный/партийный контроль. | EX-04/05, различие длины 1 мм, повторное нажатие подтверждения, перезапуск, исправление уже потреблённого выпуска. Нельзя дважды выдать или списать одно количество. История не переписывается. |
| M9. Приёмка и выпуск V1 | M1–M8, реальные формы | Сквозная проверка, нагрузка, чистая Windows, офлайн, резервные копии, обновление с сохранением данных, перенос приложения и проекта на другой ПК. Исправление дефектов и выпуск self-contained ZIP. | Протокол по согласованным критериям, EX-01–05 проходят, известные ограничения перечислены, архив запускается без установки зависимостей, обновление и восстановление проверены на копиях. Владелец принимает выпуск. |

Внутри этапов экспортный механизм можно проверить раньше, но маршрут проектируется
сначала блок-схемой, затем иллюстрациями. Переносимый web-ZIP проверяем уже в
M0/M1: не откладывать обнаружение проблем запуска и обновления до M9.

## Следующая версия и эксперименты

Не включать в обязательную V1: LAN/совместное редактирование, роли/утверждение,
плаз 1:1, кабельный тестер, QR/штрихкоды, SVG-экспорт, расширенную проверку двойного
обжима. Это согласованные будущие направления, не обещание реализовать их сейчас.
Не добавлять закупки, складскую адресацию, ERP или автоматическое межпроектное
планирование под предлогом учёта остатков.
Автоохват и подбор артикула — отдельные отключаемые эксперименты M4.
Полноценный универсальный переключатель плоского/объёмного вида не считать
утверждённым обязательным компонентом до оценки M0/M4.

## Покрытие требований

Это карта разделов, не полная поэлементная спецификация. Перед задачей берётся
точное правило из requirements-notes, затем связывается с тестом и коммитом.

| Раздел рабочих требований | Этапы |
|---|---|
| Границы, проекты, web-ZIP, офлайн, восстановление | M0, M1, M9 |
| Источники, шаблоны, каталоги, соединители, терминалы | M2, M3, M4 |
| Электрическая модель, геометрия, слои, размеры, отмена | M1, M3, M4 |
| Провода, скрутка, разделка, аксессуары, маркировки | M4 |
| Полуфабрикаты, зависимости, состояния этапов | M5 |
| Время, оборудование, передача, критический путь | M6 |
| Формы, фото, экспорт и комплекты | M7 |
| Задания, потребность, факт, остатки, корректировки | M8 |
| Прозвонка, серийные номера и результаты | M7, M8 |
| Проверка полной V1 и обновлений | M9 |

## Посильные карточки работы

Одновременно одна активная реализационная карточка. Карточка — одна проверяемая
возможность или один риск, не «сделать редактор» или «сделать всю БД».
Если задача требует перестройки нескольких модулей, сначала выделить договор
между ними и разбить работу. Уменьшать объём заранее, не после потери контекста.

Обязательные поля:
- ID, цель, статус, этап и зависимости;
- точные исходные требования и принятые решения аудита;
- что входит и что явно не входит;
- затрагиваемые модули/файлы, формат данных и влияние на миграции;
- демонстрация результата и проверки, включая один важный ошибочный случай;
- фактический результат, команды проверок, коммит, ограничения и следующий шаг.

Примеры первых карточек после согласования:
1. M0-01: таблица владения полями и контрактов источников — отчёт пробелов без записи в источник.
2. M0-02: эксперимент направляющей с повторяемой текстурой, изменением формы и выключением привязки.
3. M0-03: поиск на синтетической базе из десятков тысяч записей и замер одного нагруженного холста.
4. M0-04R: переносимый web-хост, безопасное хранение, self-contained ZIP и тест на Windows.
5. M0-05: архитектурное решение, численные цели быстродействия и подробные карточки M1 на согласование.

Эксперимент может завершиться отрицательным результатом; он считается полезным,
если ограничение воспроизведено и есть конкретная альтернатива. Не объявлять
неудачный эксперимент работающим продуктом и не разрастать его в приложение.

## Проверки и защита данных

- Расчётные тесты: единицы, округление только конечного результата резки,
  q/K, max вместо сложения, постоянные и погонные операции, пусто/ноль по контексту.
- Интеграционные: одна команда модели обновляет оба вида; смена справочника не
  меняет закреплённую версию; вспомогательные виды и полуфабрикаты не дублируют сырьё.
- Учёт: выпуск/потребление атомарны, повтор команды не удваивает факт; исправление
  сохраняет историю, отмена чертежа не стирает подтверждённое производство (A03).
- Данные: резервная копия реально восстанавливается; миграция проверяется на копии;
  недоступный источник не заменяет локальную базу пустой; ошибки XLSX не публикуют половину импорта.
- UI: обычный жгут, 300 проводов, длинный список, клавиатура, zoom, сохранение,
  ошибки и формирование документов. Нагрузочные числа — модель нагрузки, не обещание
  60 FPS на неизвестном оборудовании. Пороги определяются M0 и принимаются владельцем.
- Секреты подключения не коммитить и не включать в переносимый архив проекта.
- Разработческие тесты на синтетических данных; реальные данные — отдельные
  согласованные проверочные наборы, не публичные fixtures по умолчанию.

## Контрольные точки с владельцем

План и A01–A03 согласованы; команда начать M0 получена 2026-09-11.
M0-01–M0-05 и M1-01–M1-15 завершены. Ограниченные карточки M2 находятся в
`docs/m2-cards.md`; результат первой карточки M3 описан в
`docs/m3-01-component-placement.md`.
Текущее состояние — work-state.md.
После M0: принять архитектуру, результаты рисков и карточки M1.
После M3: принять первый сквозной процесс; до наращивания функций устранить ошибки модели.
После M6: принять расчёт маршрута на проверяемых примерах.
После M8: принять учёт факта/остатков и корректировки.
После M9: решение о выпуске V1. Формальное ТЗ и инструкция остаются отдельными
документами и выпускаются только когда владелец прямо попросит; сборка приложения
не считается автоматически разрешением написать их.

Уточнение внешнего поведения или смена архитектуры — сначала обоснованное предложение
с последствиями и подтверждение. Обычные обратимые детали внутри принятой карточки
решать самостоятельно. Не запрашивать повторное одобрение уже принятого решения.

## Продолжение при смене контекста

Перед каждым продолжением: README → requirements-notes → requirements-audit →
development-plan → work-state → карточка текущей задачи → git status/diff.
Не перечитывать все макеты и всю переписку без необходимости. Последние уточнения
владельца приоритетнее макетов. Фактически выполненное подтверждается тестом/файлом/коммитом.

В конце ограниченной задачи: обновить work-state, записать фактические проверки,
сохранить тематический коммит. Незавершённое не помечать завершённым из-за лимита
контекста. Git — история проекта, внешние данные не отправлять автоматически.
Не запускать другие пользовательские задачи или агентов без соответствующего разрешения.


M4-18 завершён: `0.28.0-m4-18`, редактируемое назначение контакта в Э4,
видимость серии при размещении, скрытие у экземпляра и у всех компонентов
текущей схемы. Клиент 789/789, сервер 718/718, TypeScript/build, API с перезапуском
и браузер прошли. ZIP без пользовательских данных — `artifacts/m4-18/TECHMAP-GRAPHER-win-x64.zip`.
Подробности и изменённые файлы: [m4-18-contact-purpose.md](m4-18-contact-purpose.md).
Следующая новая карточка продолжает индекс M4-19. Push остаётся заблокирован
ранее полученным отказом автоматической проверки точного адреса GitHub.

M4-19 завершён: `0.29.0-m4-19`, единый визуальный стиль библиотеки, инспектора Э4
и редактора рисунков. Аудит → общие стили → компактная компоновка → проверки и ZIP
выполнены. Клиент 789/789, сервер 718/718, TypeScript/build, API итогового пакета
с перезапуском, браузер 1280×720/1000×760 и проверка чистоты архива прошли.
Протокол: [m4-19-visual-consistency.md](m4-19-visual-consistency.md).
ZIP: `artifacts/m4-19/TECHMAP-GRAPHER-win-x64.zip`. Следующая новая карточка — M4-20.

M4-20 завершён как исследование и установка скилла связанных видов по документации
harness.design. Подробности: [m4-20-harness-design-skill.md](m4-20-harness-design-skill.md).
Предложенная очередь: M4-21 — выделение существующих проводов/кабелей/соединителей
и навигация из резки; M4-22 — физические ветки и маршруты; M4-23 — таблица соединений
и спецификация с происхождением строк; M4-24 — графическая схема резки/разделки.
Скилл: [techmap-harness-views](../skills/techmap-harness-views/SKILL.md).
Реализация этих четырёх карточек пока не заявляется завершённой.

M4-21 — уточнение Чертежа по шести разделам документа владельца, аудит и обновление
скилла. [Протокол](m4-21-drawing-clarification.md). Предыдущая предложенная очередь
M4-20 заменена: M4-22 — связанное выделение; M4-23 — физические трассы/ветки и 15°;
M4-24 — оболочки/защита по веткам; M4-25 — таблица соединений, спецификация на
чертеже и позиционные выноски; M4-26 — схема резки/разделки. Код этих функций
в M4-21 не реализовывался.

M4-22 завершён: [связанное выделение](m4-22-linked-selection.md), клиент 795,
сервер 718, API и браузер. Владелец поручил продолжить до окончания M4-26;
последовательная реализация M4-23–26 разрешена без дополнительных запусков.
