# Текущее состояние работы

M4-33 завершён: экран по всему диапазону смещённых проводов, постоянная
ориентация на горизонтальном пучке, независимость основных трасс от экрана,
обычное подключение к контакту с изгибами и удобный захват у таблицы.
Версия `0.39.0-m4-33`; 827/827 клиентских и 735/735 серверных тестов,
сборки, API с перезапуском, браузер и ZIP без пользовательских данных.
Протокол: [m4-33-screen-routing.md](m4-33-screen-routing.md).
Следующая карточка не начата. Ниже сохраняется история этапов.

M4-30–M4-32 завершены: фон штриховки, удобные маркеры малых фигур,
масштаб поля, графические кнопки, массивы с контактами и пропорциональный
масштаб размещённых рисунков Э4/чертежа. Версия `0.38.0-m4-32`.
825/825 клиентских и 735/735 серверных тестов, сборки, API с перезапуском,
браузер и ZIP без пользовательских данных проверены.
Протокол и ограничения: [m4-32-drawing-scale.md](m4-32-drawing-scale.md).
M4-30 `16a14f7`, M4-31 `a9b3029`. Следующая карточка не начата.
Ниже сохраняется история предыдущих этапов.

M4-27–M4-29 завершены: поля библиотечной таблицы, растяжение смешанного
выделения, рисунки по разделам и импорт PNG/JPG/BMP/SVG/HEIC.
Версия `0.35.0-m4-29`; клиент 821/821, полный сервер 731/731,
финальные валидаторы 19/19; сборки, API с перезапуском, браузер и чистый ZIP.
Протокол и ограничения: [m4-29-drawing-targets-import.md](m4-29-drawing-targets-import.md).
M4-27 `549dc8e`, M4-28 `3116b7f`; очередная карточка не начата.
Ниже сохраняется история предыдущих этапов.

M4-26 завершён: отдельная графическая схема резки/разделки, слои A/B,
оболочка и жилы кабеля, точный расход, общие команды редактирования и Undo,
ревизия сохранения и переход на Чертёж. Версия `0.34.0-m4-26`.
817/817 клиентских и 729/729 серверных тестов; сборки, API с перезапуском,
браузер и ZIP без пользовательских данных проверены.
Протокол: [m4-26-cut-diagram.md](m4-26-cut-diagram.md).
Порученный диапазон M4-22–M4-26 завершён полностью. Новая карточка не начата.
Записи ниже сохраняют историю этапов и прежних блокеров.

M4-25 завершён: таблицы соединений/спецификации на поле, стабильные связи строк,
выноски с независимыми концами и номером из BOM. Версия 0.33.0-m4-25;
810 клиентских / 729 серверных тестов, API/браузер/ZIP. Протокол m4-25-drawing-documents.md.
Следующий и последний порученный этап — M4-26: графическая резка/разделка.

M4-24 завершён: оболочки и закреплённая библиотечная защита по интервалам ветвей.
Версия 0.32.0-m4-24; 806 клиентских тестов, сервер 726/726, API/браузер/ZIP.
Протокол m4-24-branch-coverings.md. Далее M4-25 — таблицы и позиционные выноски.

M4-23 завершён: физическая топология, общие выходы, редактируемые участки,
явные маршруты и подсветка ветвей. Версия 0.31.0-m4-23; 801 клиентский / 723
серверных теста, API с перезапуском, браузер и ZIP без пользовательских данных.
Протокол: m4-23-physical-topology.md. Далее M4-24 — оболочки и защита по ветвям.

M4-22 завершён: `0.30.0-m4-22`, связанные провода/цепи и живая карта резки
в редакторе, переход строки на Чертёж. Клиент 795/795, сервер 718/718,
сборка/манифест/API с перезапуском и браузер прошли. ZIP без пользовательских
данных: `artifacts/m4-22/TECHMAP-GRAPHER-win-x64.zip`.
Карточка: `m4-22-linked-selection.md`. По поручению владельца работа продолжается
до M4-26; следующий этап M4-23 — физическая топология и маршруты.

20.09.2026 доступ к GitHub восстановлен через Git Credential Manager, аккаунт
SaveOurSouls. Обычный HTTPS push успешно отправил M4-15–M4-21 до `bc8bc7d`
в `SaveOurSouls/TECHMAP-GRAPHER`, ветка `codex/restore-20260918-0235`.
Предыдущие записи о блокере авторизации ниже — история, блокер снят.
Для последующих отправок используется HTTPS того же подтверждённого репозитория.

M4-21 — уточнение Чертежа из Google Документа (20.09.2026).
Прочитаны шесть разделов; требования и установленный скилл обновлены и проверены:
ломаные без сплайнов, 15°, физическое ветвление по точкам, оболочки/защита,
спецификация на чертеже и связанные позиционные выноски.
Протокол: `m4-21-drawing-clarification.md`. Код приложения не менялся.
Актуальная очередь — M4-22 (выбор), M4-23 (трассы), M4-24 (защита),
M4-25 (таблицы/выноски), M4-26 (резка). Она заменяет предложение из M4-20.

20.09.2026 владелец прямо подтвердил автоматическую отправку любых законченных
изменений в `git@github.com:SaveOurSouls/TECHMAP-GRAPHER.git`. Предыдущий блокер
подтверждения адреса снят. Попытка push M4-15–M4-20: SSH 22 — тайм-аут;
SSH 443 с ключами хоста из официального HTTPS API GitHub — `Permission denied
(publickey)`; HTTPS — нет сохранённой авторизации. Изменения пока локальные;
для отправки нужна рабочая GitHub-аутентификация на этом компьютере.

M4-20 — изучение harness.design и скилл связанных видов (20.09.2026).
Прочитано 16 страниц; создан и установлен `$techmap-harness-views` для Чертежа,
Э4, таблиц соединений, спецификации, резки и связанного выделения. Источник —
`skills/techmap-harness-views/SKILL.md`; проверка скилла и установленной копии прошла.
Протокол, аудит пробелов и очередь M4-21–24: `m4-20-harness-design-skill.md`.
Код и версия приложения не менялись. Подсветка из таблиц/веток ещё не реализована;
следующая предлагаемая карточка M4-21 — общий resolver и связь существующей карты
резки с чертежом. Новые карточки не открыты этим исследованием.

M4-19 — единый визуальный стиль (20.09.2026), версия `0.29.0-m4-19`.
Библиотека, инспектор Э4 и редактор рисунков приведены к согласованным шрифтам,
отступам, кнопкам и рамкам. Справка в компактных InfoHint; исправлено растягивание
их кнопок, уплотнены панели и адаптивная компоновка. Клиент 789/789, сервер 718/718,
TypeScript/build, API с перезапуском и браузер 1280×720/1000×760 прошли.
ZIP без пользовательских данных: `artifacts/m4-19/TECHMAP-GRAPHER-win-x64.zip`.
Аудит, файлы, проверки и SHA-256: `m4-19-visual-consistency.md`.
Следующая новая карточка — M4-20. Push остаётся заблокирован предыдущим отказом
автоматической проверки точного адреса GitHub.

M4-18 — назначение контакта Э4, версия `0.28.0-m4-18`, коммит `3e7c5bf`.
Редактирование назначения, локальное и общее скрытие, наследование настроек серии
проверены тестами, API и браузером. Протокол: `m4-18-contact-purpose.md`.

M4-17 — артикул, таблица Э4 и рисунки (20.09.2026), версия `0.27.0-m4-17`.
Объединён блок артикулов с предпросмотром Э4 и рисунка справа. В конфигураторе Э4
добавлен список закреплённых рисунков с видимостью; рисунки перемещаются отдельно,
связаны с таблицей прямым пунктиром и сохраняют положение в проекте.
783 клиентских / 718 серверных тестов, API и браузерные проверки.
Карточка: `m4-17-article-drawings.md`. ZIP: `artifacts/m4-17-final/TECHMAP-GRAPHER-win-x64.zip`.

M4-16 — выделение и общие операции над рисунком (20.09.2026), версия `0.26.0-m4-16`.
Реализованы копирование/вставка со смещением, рамочное выделение полностью попавших фигур,
перемещение/поворот группы, общие свойства и Delete с общей отменой Ctrl+Z.
Проверены 779 клиентских и 714 серверных тестов, сборка, API smoke и браузерные жесты.
ZIP: `artifacts/m4-16/TECHMAP-GRAPHER-win-x64.zip`.
Протокол и ограничения: `m4-16-drawing-selection.md`.

M4-15 — редактор рисунков библиотеки (20.09.2026), версия `0.25.0-m4-15`.
Реализованы поворот вокруг геометрического центра, растяжение углов при любом повороте,
выборочные привязки к углам/контурам/касательным, палитра заливки и штриховки,
отдельная колонка артикулов с сохранением выбранных объектов,
перемещение контактов и стабильная привязка контакта к `seriesRowId` таблицы Э4.
Контракт v5 расширен `articleDrawings` и `drawingContactBindings`; старые шаблоны
без этих полей остаются совместимыми.

Проверки: клиент — 771 тест, tsc и production build; сервер — 714 тестов;
API smoke на изолированной синтетической базе прошёл размещение Э4, совпадение
`templateId/version/versionSha256/article`, терминальный refresh, restart и сохранение
рисунка/штриховки/координат контактов. Браузерная проверка подтвердила растяжение
после поворота, вращение маркером, Ctrl+Z, перемещение контакта, наследование №2
из таблицы Э4 и сохранение с повторным открытием. ZIP без пользовательских данных:
`artifacts/m4-15/TECHMAP-GRAPHER-win-x64.zip`.

Ограничения: касательные предназначены для окружностей; штриховки — общий набор
графических мотивов, а не сертифицированная библиотека обозначений материалов.
Карточка и протокол: `m4-15-drawing-editor.md`.

M4-14 — снятие общей оболочки кабеля (19.09.2026), `0.24.0-m4-14`.
760 клиентских / 708 серверных тестов, сборка и API smoke прошли. Карточка и ограничения:
`m4-14-cable-sheath-strip.md`. Ранее запланированная скрутка отложена
в пользу исправлений редактора рисунков M4-15. M4-13 завершена: коммит `129cc35` отправлен в GitHub.

M4-13 — сохранение проверенной версии в GitHub (19.09.2026). Владелец поручил
сохранять каждую рабочую версию отдельным коммитом и отправлять в
`git@github.com:SaveOurSouls/TECHMAP-GRAPHER.git`. Это заменяет прежний запрет
commit/push. Первый сохраняемый срез — совокупное проверенное состояние M4-12
(`0.23.0-m4-12`); ранее накопленные изменения входят в него целиком.
Пользовательские данные и локальные ZIP/логи не входят в Git.

Дата актуализации: 2026-09-18. Завершена M4-08 — подписи контактов и артикул терминала в Э4. Протокол: `m4-08-contact-labels.md`.
Завершена M4-07 — аудит маршрутизации, цвета и
прямого отвода экрана. Протокол: `m4-07-routing-audit.md`.
Завершена M4-05 — импорт Google Sheets, независимые пины,
предпросмотр с преднастройками и размещение Э4. Протокол: `m4-05-library-import-e4.md`.
M4-04 и восстановленный M4-03-r6 сохранены. M4-06 завершена — редактор серии по новому запросу; протокол `m4-06-series-editor.md`.
M4-07 завершена: цвет не блокируется коллизией, перемещение сохраняется,
глобальный поиск сравнивает допустимые наборы, экранный отвод прямой и следует
за экраном. Протокол и ZIP: `m4-07-routing-audit.md`.
M4-08: в Э4-предпросмотре название контакта берётся из строки серии; составной ключ терминала остаётся в данных и показывается коротким артикулом.

## Принято владельцем

- План M0–M9 и A01–A03: «Принимаем, стартуем». Старт ограниченных задач M0 разрешён.
- Итоговое ТЗ и инструкция выпускаются только по отдельной команде.

## Выполнено

- Рабочие требования, аудит, план и подтверждённые EX-01–EX-05 сохранены.
- M0-01 завершена как обследование: data-contracts.md и source-schema.md.
- M0-02 завершена: experiments/m0-02/index.html и docs/m0-02-texture-experiment.md.
  Процедурная оплётка/гофра вдоль направляющей, ручное размещение, независимая длина.
- M0-03 завершена: синтетический каталог 50 000 объектов, поиск с фильтром и
  ограниченным выводом, нагрузка Canvas на 20/300 проводах и JSON-отчёт замеров.
- M0-04R пересмотрена после нового требования web-first: обычный браузерный клиент,
  локальный ASP.NET-хост, self-contained ZIP, раздельные данные, backup и миграция.
- M0-05 завершена: выбран web-first модульный монолит, продуктовый baseline .NET 10
  LTS + React/TypeScript + собственный Canvas 2D renderer, кандидатные версии и
  заявленные лицензии, инженерные бюджеты M1–M4 и 15 карточек M1. Полный locked
  restore, транзитивные лицензии и notices подтверждаются в M1-01.
- M1-01 завершена: создан product solution на .NET 10 + React/TypeScript,
  закреплены зависимости, runtime/base-path API, автоматическая проверка пакета,
  SPDX и self-contained Windows x64 ZIP. Подробности: m1-01-product-skeleton.md.
- M1-02 завершена: один Windows writer на физический `dataRoot`, безопасный повторный
  запуск существующего экземпляра, exact loopback Host/port, HttpOnly session cookie
  и отдельная CSRF-защита. Подробности: m1-02-local-session.md.
- M1-03 завершена: SQLite schema 1, поколение с `CURRENT`/`READY`, строгая история
  схемы, один EF Core/ADO.NET unit of work и безопасная диагностика. Подробности:
  m1-03-sqlite-storage.md.
- M1-04 завершена: schema 2, стабильные UUID/инкременты проектов, ручной статус,
  до 100 упорядоченных независимых жгутов, копирование и защищённый API.
  Web-интерфейс поддерживает поиск и основные команды. Подробности:
  m1-04-projects-harnesses.md.
- M1-05 завершена: schema 3, content-addressed вложения вне SQLite, атомарная
  публикация, ссылки/метаданные проекта и закреплённые снимки характеристик.
  Подробности: m1-05-attachments-snapshots.md.
- M1-06 завершена: schema 4, ревизии, идемпотентный журнал изменений проекта,
  конфликт устаревшей ревизии и клиентское автосохранение с индикацией pending.
  Подробности: m1-06-commands-autosave.md.
- M1-07 завершена: точные доменные единицы в целых микрометрах/микросекундах
  и чистый калькулятор длины резки EX-01. Подробности: m1-07-exact-units.md.
- M1-08 завершена: чистый доменный калькулятор погонной и статичной операции,
  точные `Tmachine`, `Tperson`, `Toperation`, `q` и трудоёмкость EX-02.
  Подробности: m1-08-operation-time.md.
- M1-09 завершена: чистый калькулятор равномерной загрузки полных независимых
  рабочих мест, длительности блока и трудоёмкости EX-03.
  Подробности: m1-09-capacity.md.
- M1-10 завершена в согласованном объёме ядра: Online Backup API создаёт
  согласованный снимок SQLite schema 1–4 и только referenced-вложения; manifest,
  полная проверка staging, атомарная публикация, retention и policy primitives
  защищены от повреждений, небезопасных путей и гонок. Startup-активация policy
  остаётся M1-12. Подробности: m1-10-storage-backup.md.
- M1-11 завершена: единый строгий verifier используется backup writer, retention
  и restore; dry-run создаёт отдельный recovery-root, а подтверждённый offline
  restore сначала публикует проверенный pre-restore backup и переключает `CURRENT`
  только после DB/blobs/READY. Подробности: m1-11-storage-restore.md.
- M1-12 завершена: существующий `CURRENT` больше не мигрируется на месте;
  обязательный проверенный pre-update backup предшествует immutable journal и
  candidate generation, а `CURRENT` переключается только после транзакционных
  шагов, полной проверки, completion marker и `READY`. Startup фиксирует версию
  policy лишь после открытия новой БД. Подробности: m1-12-storage-migrations.md.
- M1-13 завершена: canonical deterministic `.techmap-project.zip` содержит
  предметный snapshot проекта со всеми жгутами, pinned records и только referenced
  CAS blobs. Все project-owned строки читаются одной SQLite read transaction;
  staging полностью проверяется и атомарно публикуется без перезаписи destination.
  SQLite и служебное состояние не экспортируются. Подробности:
  m1-13-project-export.md.
- M1-14 завершена: offline CLI импортирует проверенный `.techmap-project.zip`
  отдельным проектом schema 5, атомарно выделяет ID/increment, переназначает все
  project-owned ID, начинает с revision 0 и сохраняет immutable provenance.
  ZIP-лимиты и recovery journal защищают staging, CAS и DB commit; архив format 1
  из schema 4 остаётся совместимым. Подробности: m1-14-project-import.md.
- После пользовательской проверки исправлена модель M1-04: проект является
  контейнером без общего количества, количество задаётся отдельно для каждого жгута.
  Schema 6 переносит прежнее количество в существующие жгуты и создаёт каждому
  отдельные persistent-документы `e4`, `drawing`, `route`. Интерфейс позволяет выбрать
  любой жгут, изменить только его количество и открыть его три рабочие области.
  Копирование и project ZIP сохраняют структуру с новыми owned UUID; старый snapshot
  format 1 импортируется с автоматическим созданием рабочих областей.
- M1-15 завершена: packaged CLI получил явные режимы backup, dry-run restore и
  двухфазного полного restore. Сквозной сценарий проверил предыдущий schema 4 ZIP,
  restart, миграцию 4→6 с pre-update backup, экспорт/импорт проекта с двумя жгутами,
  пробное и полное восстановление, ошибочное подтверждение и занятый `dataRoot`.
  Подробности: m1-15-acceptance.md.
- Получена временная XLSX-копия исходной книги через браузер. Проверены заголовки,
  заполненность, кандидаты ключей и связи пяти профильных справочников.
- Зафиксированы владельцы данных, контракт снимка/адаптера и девять пробелов G01–G09.
- Источник не изменён. XLSX, сырой текст формул и производственные строки не в Git.
- Начат вертикальный срез M2-05/M2-06: schema 10 хранит отдельную библиотеку
  графических шаблонов с неизменяемыми версиями и связями с несколькими артикулами.
  В web-интерфейсе появился раздел `Библиотека` с обязательными видами Э4/чертежа,
  дополнительными видами, простыми примитивами, точками контактов и Ctrl+Z.
  Справочные данные, графический шаблон и будущий экземпляр проекта остаются
  разными сущностями. Подробности: m2-05-component-library.md.
- Контрактный срез M2-08 добавил один `logicalContactId` для точек Э4, чертежа и
  дополнительных видов, независимый выход пучка и размещение одного домена повтора
  в нескольких видах. Content schema v3 хранит текст цепи, группы типов контактов,
  артикулы серии, количества контактов по группам и несколько допустимых терминалов.
  Storage schema 14 читает неизменяемые версии v1/v2/v3; миграция 12→13 не
  переписывает существующий v2, а 13→14 расширяет индекс серии до 500 артикулов.
  UI выбирает артикул, перестраивает все виды и показывает материализованные
  строки контактов. M3-01 закрепляет выбранную версию и эти строки в независимом
  документе жгута. Подробности: m2-08-template-contacts.md и
  m3-01-component-placement.md.

## Проверки

- Read-only анализ bundled Python/openpyxl: пять таблиц, два режима чтения
  (формулы и сохранённые значения), SHA-256 источника в data-contracts.md.
- Подсчёты имеют явный фильтр кандидатов; это не промышленный валидатор.
- Проверены Markdown-ссылки и git diff --check.
- Пересчёт внешней книги не выполнялся. Product-каркас M1-01 проверен отдельно;
  предметных данных и расчётов в этой карточке ещё нет.
- Исходная точка обследования M0-01: 3d80549. Итоговые коммиты получать через git log.

Проверки M0-02: 9 тестов geometry.test.cjs прошли; синтаксис app.js проверен;
локальный HTTP вернул 200. В браузере проверены форма, отвязка, мышь/клавиатура,
прозрачность, масштаб, отсутствие длины и предупреждение о тесном изгибе.
Исходная точка M0-02: d88d8b5. Полная программа ещё не существует и не проверялась.

Проверки M0-03: 4 теста каталога прошли; синтаксис двух JS-файлов проверен.
В Chrome 152 поиск имел p95 не более 2,7 мс. При 300 проводах p95 Canvas составил
126,5 мс с перестроением текстуры и 11,3 мс с кешем. В браузере проверены поиск,
фильтр, пустая выдача, пагинация, оба объёма/режима и JSON; ошибок консоли нет.

Проверки M0-04R: self-contained win-x64 ZIP собран и запущен из распакованного
каталога при отключённом поиске установленного .NET; web UI, `/api/v1/health` и
`/api/v1/runtime-info` доступны по loopback. Эксперимент хранения: 7 из 7 тестов,
включая отказ миграции и повреждение содержимого backup. WebView2 исключён;
основной кандидат — ASP.NET Core + браузер.

Проверки M1-01: frontend 14/14, .NET 43/43, регрессия M0 20/20. Единый
семистадийный сценарий собрал ZIP 42,58 МиБ (99,25 МиБ распаковано), проверил
manifest/SPDX и запустил его из пути с кириллицей/пробелами без системного .NET
и Node в режимах `/` и `/techmap/`. Фактический UI осмотрен в браузере.

Проверки M1-02: frontend 20/20, .NET 79/79, регрессия M0 20/20, сборка без
предупреждений. Self-contained ZIP 42,60 МиБ проверен в семи режимах. Дочерние
процессы подтвердили один owner для alias-путей, независимых owner для разных
roots, результат burst 1 owner + 5 existing, освобождение mutex после kill и
takeover после смерти владельца до публикации URL. 8.3 alias на текущем тестовом
томе недоступен и отмечен явным skip; unit-тест также пропускает его только при
отсутствии короткого имени Windows.

Проверки M1-03: frontend 20/20, .NET 96/96, регрессия M0 20/20, Release build без
предупреждений. Интеграционные SQLite-тесты проверили restart, rollback sync/async,
FK, конкурирующего writer, busy timeout, PRAGMA, `CURRENT` и закрытие ресурсов.
Self-contained ZIP 45,47 МиБ (106,71 МиБ распаковано) прошёл семь portable-режимов,
включая повторный запуск с прежней БД; diagnostics вернул live SQLite 3.53.3.

Проверки M1-04: frontend 24/24, .NET 132/132, регрессия M0 20/20, Release build без
предупреждений. Проверены schema 1→2, restart, 100/101 жгут, конкурентный лимит и
инкремент, копирование, protected API и URL-префикс. ZIP 45,51 МиБ (106,8 МиБ
распаковано) прошёл семь режимов; проект и два жгута сохранили UUID/порядок после
перезапуска. Независимый review после исправления SHA-256 миграций: P1/P2 нет.

Проверки M1-05: frontend 24/24, .NET 168/168, регрессия M0 20/20, Release build без
предупреждений. Проверены дедупликация,
конкурентная публикация, missing/corrupt blob, невидимая ссылка при ошибке до commit,
restart, неизменность снимка mock-источника, копирование проекта, schema 1→2→3 и 2→3,
protected API и URL-префикс. ZIP 45,53 МиБ (106,88 МиБ распаковано) прошёл семь
portable-режимов; после принудительной остановки вложение сохранило UUID, SHA-256 и байты.
Независимый review после исправления лимита тела запроса и MIME-валидации: P1/P2 нет.

Проверки M1-06: frontend 35/35, .NET 191/191, регрессия M0 20/20, Release build
без предупреждений. Проверены duplicate/replay, stale revision, конкурентный writer,
полный rollback, миграция schema 3 с проектом/жгутом/вложением/снимком, restart,
обязательный envelope и восстановление автосохранения. Self-contained ZIP и семь
portable-режимов проверены штатным сценарием; фактический UI показал переход
«Сохранено · ревизия 0» → «Сохранено · ревизия 1» после debounce.

Проверки M1-07: frontend 35/35, .NET 209/209, регрессия M0 20/20, Release build
без предупреждений. EX-01, округление только итоговой длины, nullable/ноль,
отрицательный итог, переполнение и точные преобразования покрыты доменными тестами.
Self-contained ZIP 45,57 МиБ (106,97 МиБ распаковано) прошёл семь режимов.

Проверки M1-08: профильные Domain-тесты 30/30, все .NET-тесты 221/221,
frontend 35/35, регрессия M0 20/20, Release build без предупреждений. Проверены
EX-02, раздельные погонный и статичный входы, `max`, однократное применение `K`,
`q` и `N`, nullable/ноль, непредставимая доля микросекунды и переполнение каждой
итоговой ветви. Self-contained ZIP 45,57 МиБ (106,99 МиБ распаковано) прошёл
семь режимов.

Проверки M1-09: профильные Domain-тесты 40/40, все .NET-тесты 231/231,
frontend 35/35, регрессия M0 20/20, Release build без предупреждений. Проверены
EX-03, стабильное равномерное распределение, одно и лишние места, отсутствие
подготовки на пустом месте, nullable/ноль, недоступный комплект, границы `long`
и переполнение. Self-contained ZIP 45,58 МиБ (107 МиБ распаковано) прошёл
семь режимов.

Проверки M1-10: профильные backup/policy-тесты 24/24, все .NET-тесты 257/257,
frontend 35/35, регрессия M0 20/20, Release build без предупреждений. Проверены
schema 1–4, snapshot и гонка ссылок, exact referenced blobs, повреждение источника
и staging, строгая форма схемы, отмена, физические alias-пути, malformed manifest,
retention, смена версии, регулярный policy, повтор после сбоя, сериализация и таймер.
Self-contained ZIP 45,60 МиБ (107,06 МиБ распаковано) прошёл семь переносимых
режимов. Для дочерних Windows-процессов отключены системные окна критических
ошибок; сбои остаются доступны по exit code и stderr.

Проверки M1-11: профильные restore/backup/policy-тесты 42/42, все .NET-тесты
275/275, frontend 35/35, регрессия M0 20/20, Release build без предупреждений.
Проверены dry-run schema 1–4, запрет полного restore schema 1–3, точное
подтверждение и stale plan, data/backup/recovery leases, обязательный проверенный
pre-restore backup, blobs → DB → marker → READY → CURRENT, отмена и сбой до
switch, успешный commit при ошибке observer после switch и идемпотентный повтор.
Также проверены write-through замена `CURRENT`, отказ от junction в цепочке
вложений, сохранение чужого recovery-root при гонке публикации и безопасная
очистка при исключении непосредственно после rename commit point.
Self-contained ZIP 47,20 МиБ (107,02 МиБ распаковано) прошёл семь portable
режимов вне sandbox. Внутри текущего sandbox проверка ожидаемо останавливается на
ACL-ограничении чтения LocalApplicationData в `LocalInstanceRecord.Publish`; web
host и health до этого запускаются. Для дочерних Windows-процессов системные окна
критических ошибок подавляются до managed startup, а причина остаётся в stderr и
коде выхода.

Проверки M1-12: профильные migration/policy/storage/options-тесты 50/50, все
.NET-тесты 292/292, Release build без предупреждений. Проверены schema 1/2/3→4,
неизменность source generation, обязательный проверенный pre-update backup,
rollback внутри шага, immutable journal до candidate, recovery после journal и
после READY, восстановление отсутствующего/повреждённого `CURRENT`, fail-closed
для активного незавершённого candidate, reparse entries, повреждённого journal,
downgrade и package/schema mismatch. Версия policy фиксируется только после
успешного открытия новой БД.

Проверки M1-13: Release build прошёл без предупреждений; профильные
`ProjectExportIntegrationTests|ServerOptionsTests` и проверки startup — 29/29;
все .NET-тесты — 314/314. Проверены два жгута,
pinned record, дедупликация двух ссылок на blob, исключение лишнего blob,
побайтово детерминированный повторный экспорт, отсутствие SQLite и служебного
состояния, missing project, destination внутри физического `dataRoot`, повреждённые
blob/metadata, занятый destination и сбой до commit point. Переносимый пакет собран:
frontend 35/35, .NET 314/314, ZIP 45,67 МиБ. Созданы checkpoint приложения и
синтетический `.techmap-project.zip`; CLI подтвердил SHA-256 экспортного архива.
Полный portable-сценарий после успешной проверки tampered-пакета чистым exit code 1
упирается в известное ограничение ACL среды в `LocalInstanceRecord.OpenRecordStore`;
это не затрагивает формат или offline CLI экспорта.
Финальный hardening также проверяет busy `dataRoot` (exit 1 без ложного статуса),
удерживаемый handle каталога назначения против junction/rename race и физические
alias-пути startup-журнала, направляемые вне каталога данных.

Проверки M1-14: Release build прошёл без предупреждений; профильные
`ProjectImportIntegrationTests|ServerOptionsTests|StorageMigrationIntegrationTests|`
`ProjectExportIntegrationTests|StorageBackupIntegrationTests|StorageRestoreIntegrationTests`
— 97/97, все .NET-тесты — 350/350. Проверены независимый проект с двумя жгутами,
pinned record и вложением, remap owned ID, revision 0, immutable provenance,
повторный/конкурентный импорт и повторное использование CAS. Отдельно проверены
format 1 из source schema 4, повреждённые/лишние payload, traversal, case collision,
symlink, compression bomb, rollback до DB commit, recovery незавершённого и уже
зафиксированного журнала, а также packaged CLI без браузера и ложного success.

Проверки уточнения M1-04R: frontend 44/44, все .NET-тесты 355/355, Release build и
`git diff --check` прошли. Покрыты два жгута с разными количествами, изменение только
выбранного жгута, restart, schema 5→6 с переносом legacy-количества, три разных
документа на жгут, copy, export snapshot format 2, import с remap UUID и совместимость
со snapshot format 1. Устаревшая запись количества проекта явно отклоняется, старые
идемпотентные команды добавления жгута воспроизводят исторический результат после
последующих изменений проекта, а количество ограничено диапазоном точных целых
JavaScript. Legacy-значение выше этого диапазона при переносе в жгут и выдаче через API
клампится к максимуму без изменения исходной строки проекта.
Portable-сборка визуально проверена на создании проекта, количествах 5/12, переключении
рабочих областей и сохранении изменения 5→7 после перезагрузки страницы.

Проверки M1-15: Release build без предупреждений, все .NET-тесты 372/372,
frontend 44/44, EX-01–03 4/4. Self-contained ZIP 45,71 МиБ
(107,39 МиБ распаковано) прошёл семь portable-режимов. Отдельный packaged E2E
сохранил идентичность проекта, жгутов и шести документов при restart и миграции,
создал один pre-update backup, импортировал независимую копию и успешно выполнил
dry-run/full restore; неверное подтверждение не изменило live storage.

M2-01 завершена: контракт versioned reference snapshot сохранён в
`m2-01-reference-snapshots.md`, порядок и границы M2 — в `m2-cards.md`.
Реализованы schema 7, канонический hash материализованного candidate,
validate → publish, атомарный active pointer, локальное чтение последней
опубликованной версии, SQLite store, protected Web API и fail-closed проверка.
Все тесты решения прошли: 399/399; frontend: 44/44; Release build без
предупреждений. Проверены целостность метаданных и канонического снимка, а также
backup/full restore опубликованного каталога. XLSX mapping/preview оставлены M2-02,
отдельный Google adapter — M2-09.

M2-02 завершена: добавлен безопасный локальный XLSX-адаптер с двухфазным
preview → publish, рабочим разделом «Справочники», ручным и автоматическим
сопоставлением полей, адресной диагностикой и точным подтверждением предупреждений.
Формулы в импортируемом диапазоне, внешние связи и активное содержимое запрещены;
ввод, распаковка, строки, ячейки, diagnostics, candidate и временный кеш ограничены.
Пусто, отсутствующее значение, физический ноль и настраиваемое «не применимо»
различаются. Active snapshot не меняется при ошибке или CAS-конфликте.
Проверки: .NET 421/421, frontend 57/57, Release build без предупреждений,
locked restore и `git diff --check` прошли. Self-contained ZIP 47,52 МиБ
(114,35 МиБ распаковано) прошёл семь portable-режимов вне sandbox, включая путь
с кириллицей и пробелами и работу без установленного .NET/Node.

M2-03 завершена: schema 8 добавила сменяемую активную поисковую проекцию FTS5,
B-tree индексы, составные полевые фильтры и keyset cursor без `OFFSET`. Поиск читает
только страницу из SQLite; HMAC-курсор связан с source, запросом и снимком, а после
публикации новой базы выдаёт 409 и перезапускается. Добавлен CRUD сохранённых
фильтров с областью одного источника и canonical query v1 без materialized ID.
Миграция 7→8 backfill-ит опубликованный active snapshot; backup/restore проверяет
проекцию, FTS и фильтры. Valid canonical payload не блокируется длинными или
совпадающими после нормализации именами полей.

Проверка M2-03 на 50 000 синтетических записях: общий p95 12,20 мс, max 13,10 мс
при бюджете 100/200 мс; построение индекса 17,93 с. Проверены все сортировки,
операторы, Unicode, лимиты и повреждённые курсоры, заполненная миграция schema 7,
CRUD/restart фильтров, повреждение projection/FTS/filter hash и поиск после full
restore. После пользовательской проверки исправлена классификация внешних XLSX-связей:
пассивные HTTP/HTTPS-гиперссылки разрешены, а внешние книги, локальные файлы, OLE и
источники данных остаются запрещены. Интерфейс объясняет формат универсального
импорта; portable ZIP содержит проверочный `Examples/reference-catalog.xlsx`.
Итоговый набор: 451 серверный и 57 клиентских тестов; Release-сборка и locked
restore прошли. Self-contained ZIP 47,57 МиБ (114,50 МиБ распаковано) прошёл семь
portable-режимов вне sandbox, включая импорт встроенного примера через реальный API,
путь с кириллицей и пробелами и запуск без установленного .NET/Node.

Ускоренный срез M2-10/E-01–E-04 реализован. Для рабочей книги добавлены шесть
профилей: БД.ОП (126 записей), БД.ОБ (187), БД.ТЕР (279), БД.КОАКС (59),
первая таблица СПР.КАБ (24) и таблица AWG (22). Реальный probe сохранил варианты
дубликатов, составные ключи и массивы D/L от жилы наружу; неполные поля и чтение
сохранённых результатов разрешённых формул оформлены предупреждениями. Интерфейс
сам предлагает профиль книги, а ручной универсальный импорт оставлен дополнительным.

Schema 9 хранит один общий design-документ на жгут. Э4 и Чертёж используют одни
экземпляры соединителей и проводов, но отдельные координаты и слои. Работают
нумерованные контакты, создание и переподключение проводов, абсолютная длина,
маршрутные точки Чертежа, шаг направления 15 градусов, hide/lock/reorder слоёв,
Ctrl+Z/Ctrl+Y и автосохранение. Быстрый выход ожидает сохранение; после ошибки есть
ручной повтор. Copy и project ZIP сохраняют design, snapshot format 3 остаётся
совместимым с format 1/2. Нижняя панель выполняет серверный поиск по опубликованным
терминалам и кабелям с debounce, отменой запроса и keyset pagination; универсальные
соединители доступны до получения отдельной БД соединителей.

Проверки среза: 83/83 client, 461/461 .NET, TypeScript и production build прошли.
Self-contained пакет 49,28 МиБ (114,58 МиБ распаковано) прошёл семь режимов запуска,
включая путь с кириллицей и пробелами. В браузере проверены создание проекта/жгута,
полноэкранный редактор, две отдельные карточки соединителей, переключатели Э4/
Чертёж и категории каталога. Полная промышленная номенклатура проводов,
соединителей и аксессуаров всё ещё требует соответствующих исходных баз.

Расширен срез «Соединения Э4». Проводники сохраняются и отображаются только
ортогональными участками; у контакта поддерживается обязательный короткий прямой
выход, а внутренний сегмент ломаной можно перемещать. Завершение провода на другом
проводе создаёт видимый электрический узел и объединяет цепь; конфликт двух разных
непустых обозначений цепи отклоняется. Обычное пересечение остаётся электрически
независимым и по настройке схемы показывается без знака либо с дугой.

Для выбранных проводов добавлены всплывающие команды: два провода можно оформить
дифференциальной парой одного из двух видов с заданным шагом скрутки; один или
несколько проводов — охватить объектом «Экран» с подписью, размером и перемещаемым
положением вдоль проводов. Новые данные переживают сохранение, старые документы
получают совместимые ортогональные маршруты при чтении. Автоматизированная проверка
расширенного клиента: 121/121 тест, TypeScript и production build прошли;
`git diff --check` не выявил ошибок пробельного формата. Полная пользовательская
приёмка взаимодействия на Canvas и проверка итогового переносимого ZIP ещё нужны.

После реализации редактируемых табличных соединителей выполнен независимый аудит
устойчивости. Старые schemaVersion 1 документы нормализуются как свободные
соединители, а метаданные серии, артикула и детерминированных контактов проверяются
до сохранения. Смена артикула сохраняет общие позиции и отклоняется, если удалит
уже подключённый контакт. «Вписать в экран» поддерживает крупные таблицы до 300
контактов с масштабом ниже 25%, при этом фиксированные варианты масштаба остались
25/50/100/150/200%. Автоматическое завершение локального сервера защищено от гонки
быстрой загрузки страницы и от ошибки запуска системного браузера; постоянные
401/404 больше не вызывают фоновые повторы. Объединённая проверка: 154/154
клиентских и 472/472 .NET-тестов, TypeScript и production build прошли.
Self-contained пакет `0.3.2-e4-stability` прошёл все семь portable-режимов из
пути с кириллицей и пробелами; SHA-256 архива:
`9821D03D29692F62080B3600A7AE47C80FEA06AE9AE5BE07B26FC8E7E860D04A`.

Табличный соединитель Э4 разделён на просмотр и редактирование. В режиме
просмотра зажатие мыши на заголовке, ячейке или нижней части таблицы перемещает
весь компонент; смещение до 3 px не записывает команду. Двойной щелчок включает
поля, Escape возвращает просмотр, а выбор другого объекта сбрасывает режим.
Обозначение соединителя перенесено в заголовок колонки «№». Отмена pointer-жеста
не сохраняет позицию. Поведение вручную проверено в Release web UI: перемещение
за обычную ячейку, редактирование после перемещения и Escape работают.
Автоматические проверки: 157/157 клиентских и 472/472 .NET-тестов, TypeScript,
production build и `git diff --check` прошли. Переносимый пакет
`0.3.3-e4-drag-edit` прошёл семь portable-режимов из пути с кириллицей и
пробелами; SHA-256 архива:
`8A860958596E2A38CFD806F4328F34267C02B9373EBF70DDA48AC7676B82FFF5`.

## Ограничения и необходимые вводные

- G01–G09 требуют подготовки источника до соответствующих задач M2/M6.
  Неполные/неоднозначные значения не заменять вымышленными и не исправлять в базе.
- Не найдены отдельные базы проводов, соединителей, аксессуаров в проверенной книге.
  Их наличие в других источниках не проверено. Схемы нужны до профильных карточек
  M2-11–M2-13, но не блокируют общий импорт, каталог и редактор шаблонов M2-01–M2-10.
- Формы документов нужны до приёмки M7.
- Цифровая подпись и проверка на физически другом чистом Windows-компьютере
  остаются M9. Онлайн-аудит уязвимостей выполняется отдельно от офлайн-сборки.
- В M0-02 нет сохранения, импорта БД и реальных растровых текстур. Тесные изгибы
  диагностируются, но не исправляются автоматически. Полный список ограничений — в отчёте.

## Следующая ограниченная задача

Исследованы предложенные diagram/ECAD skills и близкие открытые проекты. Решение и
применимые правила сохранены в `e4-ecad-tools-review.md`: собственные предметная
модель и TypeScript-роутер остаются ядром; внешние редакторы и MCP в runtime/ZIP не
добавляются. Из `schematic-humanizer`, WireViz, Universal Netlist, KiCad, Netzal и
`libavoid` переняты проверяемые инварианты связности и изображения. Draw.io оставлен
кандидатом будущего экспорта, Mermaid/PlantUML — кандидатом блок-схем маршрута M5.
Добавлена каноническая проекция связности `techmap-e4-connectivity-v1`: она не
содержит геометрию и позволяет доказать, что перенос, изменение таблицы и
перетрассировка не изменили контакты, endpoints, цепи и группы. Регрессионный тест
проверяет неизменность проекции после геометрических команд и её изменение после
электрической правки. Следующее усиление Э4 — адресная диагностика невозможного
маршрута. После этого изменения клиентские тесты прошли 179/179, TypeScript typecheck
и `git diff --check` завершились без ошибок.

Следующий шаг — передать ZIP для пользовательской проверки расширенных соединений
Э4: удобства прокладки и перемещения сегментов, двух видов дифференциальной пары,
внешнего вида и перемещения экрана, режимов пересечения и создания узла ответвления.
После исправления фактических замечаний продолжаются графические примитивы, шаблоны
соединителей, размещение опубликованных проводов и первая карта резки; отдельные
профильные базы подключаются по мере получения исходников.

## Статистика

- Основной опрос: запланированных вопросов 0; формы ожидаются отдельно.
- Решения аудита: 3 из 3 приняты.
- Первые карточки M0: 5 из 5 завершены.
- M1: реализованы 15 из 15 карточек; этап завершён.
- M2: базовые M2-01–03, M2-05–10 реализованы; M2-09 ожидает пользовательской
  приёмки portable-среза. M2-11–13 ждут фактические схемы отдельных баз.
- M3: M3-01 реализована и ожидает финальной автоматической и пользовательской
  проверки portable-среза; полный первый сквозной жгут ещё не завершён.
- Ускоренный редактор: E-01–E-04 и расширенные соединения Э4 реализованы;
  расширение ожидает пользовательской проверки ZIP.
- Проверка источников: 9 пунктов подготовки/уточнения, не 9 новых веток опроса.
- Доля реализации всей системы не оценивается по числу завершённых карточек.

## Правило передачи

Перед продолжением читать README, requirements-notes, requirements-audit,
development-plan, этот файл и текущую карточку; проверять git status/diff.
После каждой задачи записывать фактически выполненное, проверки, ограничения,
следующее действие и тематический коммит. Не выдавать критерии будущих тестов
за результаты. Не запускать другие задачи или агентов без разрешения.

Правила трассировки Э4 расширены. Новый ортогональный трассировщик обходит прямоугольники
всех соединителей, сохраняет обязательный прямой выход из контакта, разрешает только
перпендикулярные пересечения разных проводов и запрещает коллинеарное наложение,
нарушение зазора, самопересечение и самоналожение. При переносе таблицы transient-
предпросмотр сразу перемещает единственный экземпляр компонента и пересчитывает
провода без команд в истории; после отпускания мыши записывается одна команда.

Обозначение возвращено в верхнюю полосу, выделено и редактируется там после двойного
щелчка. Артикул расположен в нижней ячейке колонки «№». Ширины колонок вычисляются
из видимого содержимого единообразно для Canvas и HTML, с ограничением чрезмерно
широких значений. Повторяющиеся обозначения сохраняются, конфликтующие таблицы
получают красную отметку, а список ошибок позволяет выбрать и центрировать объект.
Escape действительно отменяет ввод обозначения или артикула. Геометрическая
нормализация выполняется на общей границе API при чтении и сохранении, поэтому
устаревшая автоматическая трасса не попадёт на сервер в обход экрана редактора.
Автоматические проверки: 176/176 клиентских и 472/472 .NET-тестов; TypeScript и
production build прошли. В Release web UI вручную подтверждены добавление и
компактное отображение таблиц, редактирование обозначения по двойному щелчку,
отмена через Escape, принятие дубликата, две строки ошибок и снятие диагностики.
Portable-пакет `0.3.4-e4-routing` прошёл семь режимов запуска из пути с кириллицей
и пробелами. ZIP: 49,37 МиБ; SHA-256:
`6C5C5E3879A5F43E8A4648B977D9189859DA2DFD810F7F89F653F63E6E642853`.
Пользовательская приёмка жестов переноса и живой перетрассировки ожидается.

Следующий пользовательский срез Э4 реализует восемь правок по оформлению и
редактированию соединений. Обозначение провода стало отдельным перетаскиваемым
объектом: положение хранится как доля полной ортогональной трассы и сохраняется
после перетрассировки; автоматический роутер учитывает подписи других проводов как
препятствия. Длинный артикул в нижней строке таблицы больше не расширяет колонку
«№». В демонстрационный каталог добавлена полная серия корпусов JST XH из
предоставленной таблицы, включая XHP-2(10.0)-U и XHP-6(5.0)-U, а также три
совместимых терминала SXH с диапазонами AWG, площади сечения и диаметра изоляции.

Рисунок дифференциальной пары разрежен прямыми участками; щелчок по рисунку или
экрану выделяет все связанные провода, после чего доступна команда разгруппировки
без удаления проводов. Экран рисуется овалом без текстовой подписи. Щелчок по
доступному контакту из режима выбора сразу начинает построение провода. Мостик
пересечения перекрывает исходную линию своими краями и визуально не отрывается.
Автоматические проверки перед упаковкой: 188/188 клиентских и 472/472 .NET-тестов,
TypeScript и production build прошли. Portable-пакет `0.3.5-e4-connections`
прошёл все семь режимов запуска, включая путь с кириллицей и пробелами. ZIP:
47,74 МиБ; SHA-256:
`31D77F44A3B69E0B58B21CA749E0C5774C04245D65E69F2B85357F69F666963A`.
Пользовательская проверка жестов и графического представления остаётся последним
шагом текущего среза.

Следующий срез уточняет представление проводов и таблицы соединителя по новым
референсам. Мостики пересечения сохраняют непрерывный цвет обоих проводников и
получили минимальный зазор 21 единица с учётом полного цветного контура и светлой
обводки. В рисунке дифференциальной пары цвет теперь следует за физическим проводом
через каждый X-мотив и прямой участок между мотивами.

Встроен начальный справочник проводов UL1061 (30/28/26/24 AWG) и НВ-4
(0,05/0,08/0,12/0,2/0,35 мм²). При вводе в ячейке рядом появляется фильтруемый
список, понимающий марку, AWG и размер с точкой или запятой. Введены тринадцать
классических цветов, пользовательская палитра и необязательный второй цвет с
диагональным образцом. Пользовательские цвета сохраняются в документе жгута и не
исчезают из списка после замены последнего использовавшего их контакта.

Кнопка добавления строки перенесена в верхнюю полосу, обозначение отцентрировано,
а нижняя строка объединена и показывает код библиотеки и полный артикул, не меняя
ширину колонки «№». Таблица учитывает длину нижней строки в своей общей ширине.
Автоматические проверки: 194/194 клиентских теста; TypeScript, production build и
`git diff --check` прошли; 472/472 .NET-теста также успешны. В Release web UI
вручную проверены создание свободного соединителя, редактирование по двойному
щелчку, поиск `нв 0.12`, выбор `НВ-4 0,12 мм²`, выбор красного и синего цветов и
диагональный двухцветный образец. Portable-пакет 0.3.6 прошёл все семь режимов
запуска. Следующий шаг — пользовательская проверка цветов, подсказок и пересечений
на реальной схеме.

Срез `0.3.7-m2-component-library` закрыл текущие замечания E-03 и начал
M2-05/M2-06. Экран получил собственную проводящую точку; зависимые провода
перетрассируются вместе с ним. FREE добавляет строку первым щелчком и хранит
редактируемый код отдельно от артикула. Палитра закрывается после выбора и не
перехватывает следующую ячейку. Соединители магнитно выравниваются по первой строке
контактов и соответствующей стороне. Поиск маршрута через обязательные точки теперь
глобально минимизирует длину и число изгибов.

Schema 10 и новый раздел `Библиотека` сохраняют отдельные графические шаблоны,
неизменяемые версии, виды Э4/чертежа, дополнительные виды, простые примитивы,
контактные точки и связи с несколькими артикулами. 224/224 клиентских и 481/481
.NET-тестов прошли. В Release ZIP вручную проверены создание, две версии и повторное
открытие шаблона. Семь portable-режимов прошли из пути с кириллицей и пробелами.
ZIP: 47,8 МиБ; SHA-256:
`3D3776A7F7425EF7615AEFAA329201F8E1B7D9872C05DEA287F6BEB572B438F6`.
Следующая карточка — продолжение M2-06: слои, расширенные примитивы и изображения
через CAS; затем M2-07 параметризует повторяемые контакты серии.

Срез `0.3.8-m2-template-assets` продолжает M2-06. Schema 11 хранит неизменяемые
ссылки версий шаблона на PNG в общем CAS. Web-библиотека загружает, показывает,
повторно открывает и удаляет изображения; каждая мутация создаёт версию. Backup и
dry-run restore включают глобальные library assets, а project export не втягивает
весь глобальный каталог. Старые непривязанные CAS-файлы очищаются при запуске под
эксклюзивной блокировкой dataRoot. Поддерживается проверяемое non-interlaced PNG
до 10 МиБ; другие растровые форматы пока отклоняются.

Параллельно подготовлен строгий контракт schema v2: логические контакты отделены
от точек видов, root repeat domain имеет отдельные per-view placements, а instance
occurrence получает детерминированный ключ. Клиентская модель и серверный валидатор
пока изолированы от действующего v1. Следующий шаг M2-06 — подключить v2 к store/API,
перевести редактор на слои и дать размещать загруженный PNG как примитив.

Срез `0.3.9-m2-template-v2` завершает текущую поставку M2-06. Storage schema 12 и
API сохраняют неизменяемые версии content schema v1/v2; открытие v1 выполняет явное
преобразование в памяти, а сохранение публикует отдельную версию v2. Редактор
переведён на слои, логические контакты и точки видов. PNG размещается в активном
слое как подложка с рамкой, crop, прозрачностью и порядком; `content.assets`
атомарно согласован с реальными ссылками версии.

Объекты перемещаются с live preview и одной undo-записью. Смещение хранится в
transform, поэтому результат совпадает с preview при rotation/scale и не разрушает
параметризованную геометрию. Блокировки, скрытие и порядок слоёв работают через
immutable-команды; сложные узлы открываются read-only без потери данных. Проверки:
275/275 клиентских тестов, TypeScript, production build, 511/511 .NET-тестов и
`git diff --check`. Следующие карточки: M2-07 — параметры и повторяемые контакты
серии; M2-08 — общий `logicalContactId` во всех представлениях компонента.

Срез `0.3.10-m2-template-repeat` завершает M2-07. Ширина/высота
прямоугольника или изображения и радиусы эллипса превращаются в именованные
параметры с единицей, начальным значением и диапазоном. Один прототип фигуры и
точки контакта разворачивается в 2, 10 или произвольное допустимое число
экземпляров с детерминированными ключами, номерами и шагом по X/Y. Физические
копии в шаблоне не сохраняются; домен повтора удаляется сразу из всех видов,
оставляя исходные фигуры и точки.

Клиент и сервер разрешают все числовые параметры через безопасный AST и одинаково
проверяют тип, диапазон, целочисленность, циклы, деление на ноль, итоговое смещение,
коллизии номеров и общий бюджет материализации. Независимое финальное ревью P1/P2
не выявило. Проверки: 319/319 клиентских и 519/519 .NET-тестов, TypeScript,
production build и `git diff --check`. Portable-пакет прошёл семь режимов запуска
из пути с кириллицей и пробелами. В упакованной версии вручную проверены создание
именованной ширины 180 мм с диапазоном, изменение до 220 мм и перестроение Canvas,
повтор 2→10, сохранение, повторное открытие и удаление домена повтора. ZIP:
47,92 МиБ; SHA-256:
`68F956145E3CFDA3EFB51990F4FCB3AF118044DBBE52E5E54F66FA80B07077A5`.
Следующая карточка — M2-08: общие логические контакты между Э4, чертежом и
дополнительными видами компонента.

Контрактная часть M2-08 реализована в content schema v3 и storage schema 14. Один
логический контакт связывает точки разных видов без копирования предметной
сущности; повторяемый домен получает отдельное размещение в каждом виде, а общий
выход пучка остаётся самостоятельным графическим портом. Серия хранит артикулы,
группы контактных типов, достижимое количество контактов и допустимые терминалы.
Старые v1/v2 читаются без изменения и преобразуются в v3 только явной командой.

Строгие клиентский и серверный валидаторы проверяют ссылки, уникальность, параметры,
повторы, assets и общий UUID namespace. Выбор артикула в библиотеке вычисляет
параметры повтора, перестраивает Canvas всех видов и показывает устойчивые строки
контактов с цепью и допустимыми терминалами. Профильные и полные наборы тестов,
прямые миграции 12→13→14, сборка без предупреждений и `git diff --check` прошли.

Закрепление версии шаблона и материалация его строк в документе проекта относятся
к последующему сквозному срезу M3. Заливка контактов дополнительных изображений
цветом провода также остаётся будущей функцией, поскольку требует проектной цепи.

Portable-пакет M2-08 собран и проверен в семи режимах запуска, включая путь с
кириллицей и пробелами, миграцию schema 12→13→14, backup/restore и завершение
серверных процессов. ZIP: 47,97 МиБ (50 297 322 байта); SHA-256:
`937A6E58EA40877015404BB93BFFC529ABFE319DA450C26858F620985CA41CDF`.
Пользовательская проверка серии с вариантами на 2/10 контактов и несколькими
допустимыми терминалами остаётся отдельным приёмочным шагом.

M2-09 завершена как read-only адаптер публичной Google Sheets. Пользователь может
переключаться между ручным XLSX и Google Sheets, выбрать один из шести известных
профилей и выполнить общий конвейер preview → diagnostics → подтверждение
предупреждений → публикация. Адаптер не использует OAuth, cookies или запись в
источник; URL и ID книги не сохраняются. Ошибка сети или источника не меняет
активный локальный снимок. Сетевой клиент ограничен разрешёнными Google hosts,
ручными redirects, DNS/IP-фильтром, timeout и лимитом 25 МиБ; экспорт повторно
проходит защиту XLSX от внешних книг, макросов и активного содержимого.

Реальная опубликованная книга с профилем `БД.ОП` дала 126 записей и 8 ожидаемых
предупреждений о сохранённых результатах формул. Клиент прошёл 400/400 тестов,
.NET — 599/599, Google Sheets security/API suite — 65/65. TypeScript, production
build, locked restore, семь portable-режимов и сквозной сценарий M1-15 успешны.
Финальный ZIP: 47,99 МиБ (50 321 779 байт); SHA-256:
`00325E603EED399358A6D04D05AA9BE1F118E67ED322E76BB178B5D192D3937B`.
Следующий рабочий срез — M3: размещение закреплённой версии библиотечного компонента
в выбранном жгуте и первый сквозной цикл Э4. M2-11–M2-13 подключаются после
получения схем отдельных баз и не блокируют этот срез.

M3-01 собрана для пользовательской portable-проверки. Выбранный артикул опубликованного шаблона
v3 материализуется в Э4 как экземпляр с закреплёнными ID/версией/SHA-256 шаблона,
контактами, допустимыми терминалами, представлениями и assets. Структура экземпляра
остаётся библиотечной: код, артикул, номера, типы и количество строк неизменяемы;
обозначение, цепь, провод, цвета, состояние подключения и совместимый терминал
редактируются только в выбранном жгуте.

Storage schema 15 хранит неизменяемые project-owned снимки и размещения. Одна
серверная команда атомарно сверяет ревизию Э4 и привязку к опубликованной версии,
записывает снимок/размещение и добавляет компонент в документ. Идемпотентный
`commandId` допускает безопасный повтор после потерянного ответа. Обычное сохранение
обновляет данные активного размещения, а удалённый из Э4 компонент исключается из
его списка. Подмена ID экземпляра, хэша версии или артикула отклоняется без
частичной записи.

Project export поднят до snapshot format 4 и включает только достижимые из
документов жгутов снимки, размещения и assets. Копирование и export/import
переназначают project-owned ID компонентов, контактов и ссылок проводов, сохраняя
идентичность версии шаблона и выбранного артикула. Отдельный сценарий проверки —
`m3-01-component-placement.md`.

Portable-метаданные подняты до `0.4.0-m3-01`, storage schema 15. Скрипт упаковки
получил параметр `-ArtifactSlice` с безопасным значением `m3-01`. TypeScript,
production build и 430/430 клиентских тестов прошли; полный .NET-набор — 609/609.
ZIP прошёл семь portable-режимов, включая путь с кириллицей и пробелами. Сквозной
M1-15 подтвердил миграцию предыдущего пакета до schema 15, pre-update backup,
импорт проекта и packaged restore. Финальный ZIP: 48,04 МиБ (50 376 618 байт),
SHA-256:
`BC91576BD1C52BAF04C55AA6DF94DFCA01ED12577B875C831093E36DB8D3231D`.
Следующая карточка M3 продолжает сквозной жгут после пользовательской проверки
этого среза, не ожидая отсутствующие промышленные профили M2-11–M2-13.

По пользовательской проверке M3-01 подготовлен corrective-срез
`0.4.1-m3-01-review`. Исправлены попадание в графический X неподключённого
контакта, взаимодействие с полями и списком проводов, сетка образцов цветов,
предпочтение параллельного автоматического маршрута, повторная оптимизация после
привязки/удаления, сохранение допустимой ручной геометрии при движении блока и
удаление перегиба двойным щелчком. До упаковки TypeScript и 439/439 клиентских
тестов прошли. Полный .NET-набор — 609/609. Corrective ZIP успешно прошёл семь
portable-режимов и M1-15 с миграцией, backup, импортом и restore. Архив выпущен
отдельно в `artifacts/m3-01-r1`: 48,04 МиБ (50 378 832 байта), SHA-256:
`F2030850A4A03313051A9F7D4DBF7B30DA4568D64F382AF93C497D1FB237DE36`.

Второй corrective-срез M3-01 выпущен как `0.4.2-m3-01-r2`. Исправлена
визуальная коллизия дифференциальной пары с третьей цепью и гонка между
автосохранением документа и выходом к проекту. Идентичный повтор сохранения
идемпотентен, но отличающееся содержимое со старой ревизией остаётся конфликтом.

Одновременно начата M3-02: API жгута читает точные закреплённые project-owned
снимки библиотечных компонентов и ограниченные жгутом PNG; клиент проверяет весь
граф до использования. Отрисовка этих видов в редакторе остаётся следующей частью
M3-02 и описана в `m3-02-library-instance-views.md`.

Проверено 452/452 клиентских и 610/610 .NET-тестов, production build, семь
portable-режимов и M1-15 с миграцией, backup, импортом и restore. Архив
`artifacts/m3-01-r2/TECHMAP-GRAPHER-win-x64.zip`: 48,05 МиБ
(50 385 070 байт), SHA-256:
`FC62D8F65B7EFAC3D13FF350DE0A2F12C9A2BAAA79E5A5175D7F3F8AA5B41EAF`.

M3-02 подключает к редактору полное графическое содержимое точного project-owned
снимка библиотечного компонента. Различимые виды Э4 и чертежа рисуются из слоёв,
групп, параметров, повторов, примитивов и PNG закреплённой версии; новая версия
глобальной библиотеки не меняет экземпляр жгута. Материализованные логические
контакты определяют положение и направление проводов в обоих видах, включая
строгое сопоставление повторов. Границы библиотечной графики участвуют в
выделении, перемещении и «вписать в экран». При недоступном PNG видна заглушка,
а при непригодном виде или ошибке графа остаётся существующий fallback.

После независимого ревью добавлены согласованный fallback контактных точек,
повтор загрузки графа, проверка встроенного снимка по project-owned содержимому,
hit-test внешнего знака X и правильный порядок PNG-подложек. Прошли 470/470
клиентских тестов, TypeScript, production build и 610/610 тестов .NET.

Portable-срез `0.5.0-m3-02` прошёл семь режимов запуска и M1-15 с миграцией до
схемы 15, backup, импортом и restore. Архив
`artifacts/m3-02/TECHMAP-GRAPHER-win-x64.zip`: 49,69 МиБ
(52 108 220 байт), SHA-256:
`0677F4540FD5DEEFA8F1B78B9C74859C5EA9355E5C32238C2F0AA350E38A824F`.

Corrective-срез M3-02-r1 исправляет заполнение серии в библиотеке. Поле артикула
явно задаёт итоговое количество контактов и сразу объясняет, из каких фиксированных
контактов и повторяемого сегмента шаблон может его получить. Без домена повтора
допустимо только фактическое число фиксированных контактов. Встроенная памятка
показывает порядок создания прототипа, повтора, связанных точек видов, артикула и
терминалов. Сетка серии, карточки групп и строки терминалов адаптируются к ширине
редактора и не расширяют страницу за пределы окна.

Срез `0.5.1-m3-02-r1` прошёл 471/471 клиентский тест, TypeScript, production
build, 610/610 тестов .NET, семь portable-режимов и M1-15. Архив
`artifacts/m3-02-r1/TECHMAP-GRAPHER-win-x64.zip`: 49,70 МиБ
(52 112 135 байт), SHA-256:
`64218A32034D3BA6460B05A4FB024646BB8C6D66DC868C786A1EABC3EC649773`.

M3-03 добавляет первый сквозной расчёт резки провода. Новый провод сохраняется с
неизвестной длиной; инспектор принимает абсолютную длину, две знаковые поправки
концов и шаг округления. Расчёт в целых микрометрах округляет вверх только итог,
а незавершённый провод остаётся в проекте с предупреждением и не создаёт расход.
Read-only API и раскрываемая таблица выбранного жгута показывают длину резки,
число заготовок по индивидуальному количеству жгута и общий метраж. До закрепления
записи БД.ПР таблица явно имеет ограниченный статус и не считается спецификацией
материалов. Инструкция заполнения шаблона вынесена в отдельный документ; встроенная
памятка уточнена в соответствии с фактическим UI одного примитива-прототипа.

Срез `0.6.0-m3-03` прошёл 485/485 клиентских тестов, TypeScript, production
build, 613/613 тестов .NET, семь portable-режимов и M1-15 с миграцией до схемы
15, pre-update backup, импортом проекта и packaged restore. Архив
`artifacts/m3-03/TECHMAP-GRAPHER-win-x64.zip`: 48,08 МиБ
(50 419 856 байт), SHA-256:
`3601B654FDE6A800F7D6ED1EC91CB3922686E730E741532FA22D04FC8D757A47`.

Corrective-срез `0.6.1-m3-03-r1` закрывает критические дефекты Э4 после
пользовательской проверки. Документ больше не обнуляется при ошибке производной
геометрии: повреждённый маршрут восстанавливается отдельно, серверная версия
сохраняется при конфликте ревизий, а несохранённый локальный черновик предлагается
восстановить явно. Рероутинг нормализует координаты и нулевые сегменты, выдерживает
зазор между проводами и углами, не ломает допустимые ручные маршруты и устойчиво
обрабатывает двухконтактный crossover.

Экран теперь поддерживает вывод «сверху», «снизу» и «с двух сторон», запоминает
сторону подключения провода и перемещается по любому общему параллельному участку
маршрутов с живым предпросмотром. Границы экрана учитывают оба вывода. Проверены
498/498 клиентских тестов, TypeScript, production build, 613/613 тестов .NET и
`git diff --check`; portable-проверки выполняются при упаковке этого среза.

Corrective-срез `0.6.2-m3-03-r2` добавляет восстановление локального черновика
после ошибки сохранения, завершает исправления рероутинга и экрана, упрощает
диагностику импорта справочников и вводит первый рабочий этап заполнения серии:
типы контактов, единичное и массовое добавление артикулов по шаблону. Шаблон
`PHR-XX` со значениями `1-14, 16, 18-20` разворачивается атомарно, а диапазоны
вида `01-14` сохраняют ведущие нули. Полноценное редактирование и persistence
таблицы Э4 schema v4 продолжается следующим срезом.

Проверены 525/525 клиентских тестов, TypeScript, production build и 613/613
.NET-тестов. Архив `artifacts/m3-03-r2/TECHMAP-GRAPHER-win-x64.zip`: 49,73 МиБ
(52 147 949 байт); SHA-256:
`E2082C02A0839F935E3AEA73E2FCDB9886F71D25E09A1AA616B9E7D7B1BB984F`.

M3-04 начинает полноценный табличный контракт соединителя Э4. Content schema v4
хранит фиксированные колонки `№`, `Назначение`, `Цепь`, `Тип` и `Стандартный
контакт`, общие значения серии и локальные переопределения каждого артикула.
Редактор позволяет применять правку к одному артикулу или всей серии, скрывать
колонки и выбирать только совместимый с типом терминал. Клиент и сервер строго
сверяют таблицу с типами, артикулами, количеством контактов и envelope bindings.

Storage schema 16 расширяет неизменяемые версии глобальной библиотеки до content
schema v4, сохраняя чтение v1–v3. Проектные snapshots и placement пока остаются
v3-gated: переход экземпляров проекта на v4 выполняется отдельным безопасным
срезом, чтобы не менять одновременно глобальную библиотеку и существующие проекты.

Проверены 533/533 клиентских и 622/622 .NET-теста, TypeScript, production build
и семь portable-режимов. Архив `artifacts/m3-04/TECHMAP-GRAPHER-win-x64.zip`:
49,76 МиБ (52 172 692 байта); SHA-256:
`FF5E33690ACA8B2C29CFC5FB7F86FD29DAEFDEB20C29BF5200F76F7B81859947`.

Corrective-срез `0.7.1-m3-04-r1` исправляет проверенные пользователем детали
редактора Э4. Экран имеет отдельные явные точки подключения на верхней и нижней
частях овала; провод создаётся пользователем от выбранной точки, а отключённая
сторона больше не подменяется другой. Параметры длины и резки убраны из
инспектора Э4 и сохранены в чертеже. Слои получили подписанную кнопку
`Скрыть`/`Показать`.

Инспектор провода предлагает стандартные цвета справочника и сохраняет доступ к
полной палитре. Цвет прямого провода контакт–контакт следует за последней правкой
цвета его контакта; ответвления через узел или экран и явно перекрашенный провод
остаются независимыми. Старые прямые провода получают источник цвета при чтении,
поэтому исправление применяется и к существующим проектам.

Проверены 539/539 клиентских и 622/622 .NET-теста, TypeScript, production build
и семь portable-режимов. Архив
`artifacts/m3-04-r1/TECHMAP-GRAPHER-win-x64.zip`: 49,76 МиБ
(52 174 771 байт), SHA-256:
`4A58120EFC1695B88583E5D156D3B8360633422904D9E9943C94604D7A213DE0`.

M3-05 завершает сквозное размещение табличных шаблонов Э4 schema v4. В каталоге
редактора показывается одна карточка серии, первый артикул используется при
размещении, а другой артикул этой же серии выбирается в инспекторе. Смена артикула
сохраняет пользовательские значения совпадающих строк и блокируется, если она
удалит уже подключённый контакт. Проектные snapshots, обычное сохранение,
экспорт, импорт, копирование и backup поддерживают v3 и v4; storage schema 17
без изменения данных расширяет прежнее ограничение project snapshots.

Редактор серии показывает артикулы строками одной таблицы и создаёт отдельную
колонку количества для каждого типа контакта. Артикулы можно искать в локальном
справочнике `technology-connectors`, добавлять вручную или разворачивать шаблоном.
Справочники получили автономный табличный редактор: пользователь выбирает или
создаёт источник, добавляет строки и текстовые поля и хранит для каждого поля имя
столбца внешнего источника для последующей синхронизации. Изменение публикуется
как новая неизменяемая версия справочника.

Проверены 548/548 клиентских тестов, TypeScript, production build, 624/624
.NET-теста и семь portable-режимов. Архив
`artifacts/m3-05/TECHMAP-GRAPHER-win-x64.zip`: 49,77 МиБ
(52 190 417 байт), SHA-256:
`86661F241C26FC46C1D3AA7A0ECDB2A58838C415F89AF134FC1357BB43D18B11`.

M3-05 corrective `0.8.1-m3-05` подключает активный справочник
`technology-terminals` к настройке артикулов серии. Допустимые терминалы можно
искать и добавлять из БД.ТЕР, сохранив ручной ввод. Для выбранного артикула и
типа контактов назначается один стандартный терминал сразу всем строкам этого
типа; удаление его из допустимого набора атомарно очищает несовместимое
назначение. Построчный выбор стандартного терминала всегда остаётся в области
выбранного артикула, поэтому режим общей правки таблицы не может записать
несовместимый терминал другим артикулам серии.

Проверены 552/552 клиентских теста, TypeScript, production build, 624/624
.NET-теста и семь portable-режимов. Архив
`artifacts/m3-05-r1/TECHMAP-GRAPHER-win-x64.zip`: 49,78 МиБ
(52 193 617 байт), SHA-256:
`BD4EF6655776111F27230E2A22634FD9DA155A4B866279C22974EEA06554D20B`.

M3-06 `0.9.0-m3-06` доводит первый рабочий проход редактора вспомогательных
видов до поведения графического редактора. Прямоугольник и эллипс выбираются
кликом по площади, линии имеют расширенную область попадания. У выбранной фигуры
показываются восемь маркеров изменения размера, у линии — две конечные точки;
один жест создаёт одну запись отмены. Параметризованные и повёрнутые объекты пока
защищены от интерактивного resize и продолжают редактироваться через параметры.

Правая панель получила заливку, цвет и толщину линии, быстрые цвета и четыре типа
штриха. Эти стили сохраняются в шаблоне и одинаково рисуются в библиотеке и в
размещённом компоненте. Старые шаблоны без типа штриха читаются как сплошные.
Белый провод в Э4 теперь имеет тонкий тёмный контур, включая мостики пересечений
и дифференциальные пары, поэтому остаётся видимым на белом поле.

Проверены 561/561 клиентский тест, TypeScript, production build, 625/625
.NET-тестов и семь portable-режимов. Архив
`artifacts/m3-06/TECHMAP-GRAPHER-win-x64.zip`: 48,15 МиБ
(50 491 293 байта), SHA-256:
`CB639E4C4AC026E4ADA9B71804328072C09F3AEE6EEC635070165C2FEB37AFB4`.

M3-07 `0.10.0-m3-07` исправляет структуру справочников и совместимости
терминалов серии. Экран справочников показывает слева все опубликованные
таблицы с количеством строк, а справа — выбранный справочник как обычную
редактируемую сетку строк и столбцов. Редактирование сохраняет исходный тип
источника и JSON-типы неизменённых значений.

Совместимые терминалы теперь задаются один раз для всей серии соединителя и
показываются компактной таблицей «Источник / Тип / Артикул». Таблица артикулов
содержит только артикул и количество контактов каждого типа. Формат шаблона v5
и таблицы Э4 v2 удаляет дубли терминалов из каждого артикула; существующие v3/v4
при открытии мигрируют объединением прежних списков без потери записей. При
размещении компонента общий список серии доступен в поле «Терминал» каждой
строки Э4. SQLite schema 18 сохраняет чтение шаблонов v1–v4.

Проверены 569/569 клиентских тестов, TypeScript, production build, 631/631
.NET-тест и семь portable-режимов. Архив
`artifacts/m3-07/TECHMAP-GRAPHER-win-x64.zip`: 48,16 МиБ
(50 501 058 байт), SHA-256:
`A9D18FD00746D40AEAC947EAFA48C7BC3C55356F3F38F92E0B5EED58F183F569`.

M3-08 `0.11.0-m3-08` добавляет создание и непосредственное редактирование
ломаной, кривой Безье и замкнутого контура в графических видах библиотеки.
Маркеры позволяют двигать вершины и управляющие точки; двойной щелчок по
сегменту добавляет вершину линии или контура, а по внутренней вершине удаляет
её. Замыкающий сегмент контура редактируется так же, как остальные. Один жест
создаёт одну запись отмены. Контур и замкнутая кривая получили управление
заливкой в панели свойств. Формат шаблона и SQLite schema 18 не меняются;
подробности и границы среза описаны в `m3-08-primitive-path-editing.md`.
Проверены 580/580 клиентских тестов, TypeScript, production build,
631/631 .NET-тест, `git diff --check` и семь режимов portable-пакета.
`artifacts/m3-08/TECHMAP-GRAPHER-win-x64.zip`: 48,17 МиБ
(50 507 604 байт), SHA-256:
`B5758B38675534545C2C6ADDE63926018F78B3B0F87D70C47F1A6A3AC583A579`.

M3-09 `0.12.0-m3-09` завершает согласованную базовую геометрию открытых путей:
переключаемая привязка точки к углу 15° рассчитывается в координатах вида, а
визуальный радиус изгиба линии/ломаной одинаково строится в SVG библиотеки и
Canvas размещённого компонента. Настройка привязки не входит в документ, радиус
не изменяет расчётную длину. Подробности — `m3-09-angle-snap-bend-radius.md`.
Проверены 586/586 клиентских тестов, TypeScript, production build, 631/631
.NET-тест, `git diff --check` и семь режимов portable-пакета; серверный формат
и SQLite schema 18 не менялись.
`artifacts/m3-09/TECHMAP-GRAPHER-win-x64.zip`: 48,17 МиБ
(50 511 212 байт), SHA-256:
`3E49BE42DE66D777592A9817F59D3A4AEE2423269CB64B7F208BF6F04C6779C7`.

M3-10 `0.13.0-m3-10` завершает базовые операции над несколькими фигурами.
Ctrl/Shift/Meta формируют выбор корневых объектов одного слоя; общая рамка и все
фигуры следуют за live preview, а перенос записывается одной командой отмены.
Соседние фигуры можно сгруппировать, группу безопасно разгруппировать, корневые
объекты переставить на один шаг и повернуть вокруг сохранённого геометрического
центра произвольно или на ±90°. Опасные случаи с изменением paint order, общей
прозрачностью, skew, вложенным прототипом повтора, параметрической или вырожденной
геометрией отклоняются атомарно. Безье пока не поворачивается до точного расчёта
экстремумов кривой. Подробности — `m3-10-grouping-foundation.md`.

Проверены 599/599 клиентских тестов, TypeScript, production build, 631/631
.NET-тест, `git diff --check` и семь режимов portable-пакета; формат документа и
SQLite schema 18 не менялись.
`artifacts/m3-10/TECHMAP-GRAPHER-win-x64.zip`: 48,18 МиБ (50 523 394 байта),
SHA-256 `A3CD4D2C5FE60733AC39FEF53C379CC46FA3776003F9D2BE1CC0835764075D35`.

M3-11 `0.14.0-m3-11` связывает провод схемы с точной опубликованной записью
`wire` или `cable`. Двойной щелчок по материалу назначает его одному выбранному
проводу; инспектор показывает имя и ключ, позволяет заменить или очистить
привязку, а команда участвует в отмене и сохранении. Документ хранит источник,
UUID и SHA-256 снимка, SHA-256 записи и текстовый слепок материала, сохраняя
совместимость прежних документов schemaVersion 1.

Карта резки показывает фактический материал, количество экземпляров выбранного
жгута, длину резки и общий метраж. Отсутствие материала и конечной длины выдаётся
раздельными предупреждениями; неизвестный расход остаётся пустым. Карта становится
`ready` только после заполнения всех обязательных данных. SQLite schema 18 не
менялась. Подробности — `m3-11-wire-material-cut-list.md`.

Проверены 622/622 клиентских теста, TypeScript, production build, 639/639
.NET-тестов, `git diff --check` и семь portable-режимов.
`artifacts/m3-11/TECHMAP-GRAPHER-win-x64.zip`: 48,19 МиБ (50 529 415 байт),
SHA-256 `FCFAD0A30575201555B5ADE1D5531F972D12B394EE0CA15A61ED46991F9339B6`.

M3-12 завершает автоматизированную контрольную точку первого сквозного жгута.
Один сценарий проходит от незавершённого провода через назначение материала и
длины, перемещение и переподключение до сохранения и повторного чтения готовой
карты резки. Отдельные интеграционные сценарии подтверждают те же identity,
точные десятичные значения и связи после перезапуска SQLite, копирования проекта
и export/import с ожидаемым переназначением ID компонентов. Production-код менять
не потребовалось. Подробности — `m3-12-first-harness-acceptance.md`.

Проверены 623/623 клиентских теста, TypeScript, 640/640 .NET-тестов и
`git diff --check`. Следующий этап — M4, начальный ограниченный срез послойной
разделки провода/кабеля без одновременного добавления покрытий и операций.

M4-01 `0.15.0-m4-01` добавляет независимые профили послойной разделки для начала
и конца провода. В Чертежах пользователь выбирает конец и закрепляет опубликованную
запись БД.КОАКС двойным щелчком. Инспектор показывает источник и таблицу D/L с
рассчитанными ступенями. Нумерация идёт от центральной жилы наружу, все L считаются
от общего конца; пример 2,5/3,5/7,5 даёт ступени 2,5/1/4 мм.

Модель поддерживает от 1 до 64 слоёв и пропуски индексов. Неполные или физически
неупорядоченные записи видны с диагностикой, но не назначаются. Разные профили
обоих концов сохраняются при save/reload, копировании и export/import. Старые
документы читаются без миграции, SQLite schema 18 не менялась. Подробности —
`m4-01-layered-strip-profiles.md`.

Проверены 654/654 клиентских теста, TypeScript, production build, 640/640
.NET-тестов, `git diff --check` и семь portable-режимов.
`artifacts/m4-01/TECHMAP-GRAPHER-win-x64.zip`: 48,20 МиБ (50 537 357 байт),
SHA-256 `0A404302CB8C35169A5D8888BDADBDEB872D04F063AAF6712A4FB4BA8B3875B2`.

M4-02 `0.16.0-m4-02` добавляет производное графическое отображение профилей
разделки в Чертеже. Длины и диаметры ступеней нормализуются раздельно, поэтому
точные физические D/L не смешиваются с условным масштабом холста. Каждый профиль
занимает не более 40% конечного сегмента, два встречных профиля не перекрываются,
а через угол геометрия не продолжается.

Ступени наследуют слой и идентичность провода, участвуют в выборе и границах
«вписать в экран», но не являются самостоятельными объектами и не меняют маршрут,
длину или карту резки. Слишком короткий конечный сегмент сохраняет профиль и
выдаёт понятное предупреждение. В Э4 ступени не отображаются. Подробности —
`m4-02-strip-profile-drawing.md`.

Проверены 675/675 клиентских тестов, TypeScript, production build, 640/640
.NET-тестов, `git diff --check` и семь portable-режимов.
`artifacts/m4-02/TECHMAP-GRAPHER-win-x64.zip`: 48,20 МиБ (50 540 503 байта),
SHA-256 `23191B8A2BCC006EB541A01DA0A01AFE219B7919E59C13376A45F41969E0575A`.

M4-03 `0.17.0-m4-03` вводит многожильный кабель как один физический материал,
содержащий несколько существующих электрических проводов. В Чертеже пользователь
выделяет два или более провода, создаёт кабель, назначает опубликованный материал
типа `cable` и задаёт конечную длину, поправки концов и шаг округления. Жилы
сохраняют независимые подключения и отображение в Э4.

Карта резки создаёт одну строку кабеля и исключает входящие жилы из повторного
расхода. Строгая проверка отклоняет неизвестные и повторные жилы, членство в двух
кабелях, неверный тип материала и некорректные физические параметры. Старые
документы без массива `cables` читаются как пустой состав; SQLite schema 18 не
менялась. Copy и export/import сохраняют состав, закреплённый снимок материала и
точные длины. Подробности — `m4-03-multicore-cables.md`.

Проверены 684/684 клиентских теста, TypeScript, production build, 650/650
.NET-тестов, copy/export/import acceptance, `git diff --check` и семь
portable-режимов. `artifacts/m4-03/TECHMAP-GRAPHER-win-x64.zip`: 48,21 МиБ
(50 547 076 байт), SHA-256
`414DD9CF8257B6AE65997520237A43B2840BD1D61C97F15EE920F7C5EAEE1B9B`.

M4-03-r1 `0.17.1-m4-03-r1` исправляет размещение первого реального артикула
серии и сериализацию `contactGroups` в формате v5. Редактор библиотеки получил
автосохранение в отдельный изменяемый черновик, удаление серии с подтверждением
и компактную таблицу совместимых терминалов. Тип контакта и выбор стандартного
терминала сохраняются независимо; несколько терминалов могут относиться к одному
типу. При записи или размещении черновик публикуется одной версией.

В Э4 цвет прямой цепи контакт–контакт синхронизируется с автоматическим цветом
обоих контактов. Ручное значение имеет приоритет и может быть сброшено командой
«Автоматический цвет»; ответвления из середины цепи не распространяют цвет.
Экран перемещается по полной последовательности общих участков, включая перегибы
и обратное направление. Подробности и ручная проверка —
`m4-03-r1-editor-corrections.md`. Формат документа жгута не изменился; SQLite
schema 19 добавляет хранилище черновиков шаблонов.

Проверены 707/707 клиентских тестов, TypeScript, production build, 655/655
.NET-тестов, `git diff --check` и семь portable-режимов.
`artifacts/m4-03-r1/TECHMAP-GRAPHER-win-x64.zip`: 48,23 МиБ
(50 569 093 байта), SHA-256
`AC0F95BEE67D6E91BD64211C39A51EB5BA9B7E055C59BD2AD3EB2F59F96D6FA4`.

M4-03-r2 `0.17.2-m4-03-r2` исправляет замечания C1–C4. Поиск терминалов в
библиотеке сначала использует БД.ТЕР и автоматически переходит к прежнему
источнику `technology-database`, если отдельный снимок ещё не опубликован.
При успешной проверке XLSX справочник публикуется автоматически; предупреждения
не требуют ручных галок, а технические детали собраны в компактном журнале
«Ошибки». Список загруженных таблиц и блок импорта уплотнены и размещены в
левой колонке, тогда как выбранная HTML-таблица занимает основную область.

Проверены 709/709 клиентских тестов, TypeScript, production build, 655/655
.NET-тестов, `git diff --check` и семь portable-режимов, включая путь с
пробелами. `artifacts/m4-03-r2/TECHMAP-GRAPHER-win-x64.zip`: 48,23 МиБ
(50 568 722 байта), SHA-256
`D63B59F4DDBA809F86AA2C6E29530899EED65BB0F0F7754506B7187C5BC2E670`.

M4-03-r3 `0.17.3-m4-03-r3` исправляет импорт формульных ячеек справочников.
Для обычного импортируемого поля используется сохранённое в XLSX конечное
значение формулы, а в журнал добавляется предупреждение. Формулы ключей и
заголовков, ошибки Excel и отсутствие сохранённого результата обязательного
поля остаются блокирующими, поскольку готового значения для импорта нет.

Предупреждения не имеют элементов подтверждения и автоматически передаются при
публикации. В левой панели они доступны в отдельном сворачиваемом журнале;
блокирующие ошибки показаны отдельно. Номер версии в верхней панели позволяет
отличить этот пакет от старого клиента, показанного на пользовательском снимке.

Проверены 710/710 клиентских тестов, TypeScript, production build, 657/657
.NET-тестов и семь portable-режимов. Собранные JS-файлы проверены на отсутствие
старого предварительного просмотра и элементов подтверждения предупреждений.
`artifacts/m4-03-r3/TECHMAP-GRAPHER-win-x64.zip`: 48,23 МиБ (50 567 953 байта),
SHA-256 `7E3B04940C965B00BC288321E806A6FFD442B2EC3E19878E18EEAE2BF3D0DCDA`.

M4-03-r4 `0.17.4-m4-03-r4` заменяет поочерёдную загрузку профилей публичной
Google Sheets пакетной синхронизацией. По одной ссылке книга скачивается один
раз, после чего все шесть известных профилей независимо проверяются и сразу
публикуются. Ошибка одного листа не отменяет остальные результаты. Слева
показывается компактный итог для каждой таблицы, список справочников обновляется
автоматически, а после успешной синхронизации открывается БД.ТЕР либо первый
успешный справочник.

Проверены 711/711 клиентских тестов, TypeScript, production build, 659/659
.NET-тестов и семь portable-режимов. `artifacts/m4-03-r4-final/TECHMAP-GRAPHER-win-x64.zip`:
48,23 МиБ (50 572 573 байта), SHA-256
`0FA7EE07C4DCB9A37E6F48FB61BD51C1E0F6F56CB4E85702E15D7958B9AFCD65`.

M4-03-r5 → M4-03-r6 `0.17.6-m4-03-r6`: работа возобновлена от `b05c112`
(18.09.2026 02:35:29 МСК) в ветке `codex/restore-20260918-0235`.
Размещение в Э4 использует единую привязку preview; регрессия проверяет
templateId/version/hash/article при публикации новой версии черновика.
Реальная проверка API выявила и помогла устранить отказ v5 на сервере:
SQLite schema 20, project-owned снимки, экспорт/импорт и независимая таблица Э4.

Добавлено удаление целого проекта: кнопка рядом с «Новый проект», компактное
подтверждение, справочные тексты в InfoHint, отмена по Escape, ожидание
автосохранения и проверка ревизии. Удаление транзакционное, сохраняет другие
проекты и общую библиотеку. Пользовательские данные для проверок не открывались.

Полный протокол, план этапов, ограничения, перечень изменённых файлов и итоговый
пакет — [m4-03-r6-resumed-audit.md](m4-03-r6-resumed-audit.md).
По прямому указанию пользователя коммиты и отправка изменений не выполнялись.

M4-09 `0.22.0-m4-09`: переработан редактор рисунка Э4. Схема и рисунок используют
один выбранный артикул и его параметры; рисунок доступен отдельным полноэкранным
режимом с кнопкой «Сохранить и выйти», а в предпросмотре Э4 показывается рядом со
схемой. Ctrl+Z хранит 100 состояний. Добавлены автоматические массивы 1–4 рядов,
направление вдоль/поперёк рядов, новая строка и змейка, а также замена `{{n}}` в
тексте прототипа. Комбинированные корпуса остаются ручными. Справочные тексты
переданы в InfoHint.

Проверки M4-09: клиент 740/740, TypeScript, production build, сервер Release
694/694 и `git diff --check`. Smoke размещения использует существующую
синтетическую процедуру M4-08; пользовательские проекты не открывались и не
удалялись. ZIP создаётся после финальной сборки в `artifacts/m4-09`.

M4-04 `0.18.0-m4-04`: аудит восстановления подтвердил ветку, сохранённый stash
и контрольную сумму M4-03-r6. Выявлен и исправлен приём сервером некорректных
профилей разделки. Общая проверка применяется при PUT/чтении, импорте и экспорте;
учитывает оба конца, формат привязки, 1–64 слоя, пропуски индексов и точные
положительные возрастающие D/L. Ошибка не изменяет документ или ревизию.
SQLite schema 20 и формат документа 1 сохранены, интерфейс не менялся.

Проверены 717/717 клиентских и 689/689 серверных тестов, TypeScript, production
build, настоящий API итогового EXE, `git diff --check` и семь portable-режимов.
`artifacts/m4-04/TECHMAP-GRAPHER-win-x64.zip`: 52 297 125 байт, SHA-256
`897392E23572664F4421CE759F15B320B1D37643FEE40A4B306219311014B208`.
В архиве нет пользовательских данных. Аудит, ограничения и изменённые файлы —
`m4-04-post-restore-audit.md`. Следующий ограниченный срез M4-05 — физическое
снятие общей оболочки кабеля и отображение на чертеже. М5 пока не начинается.

M4-10: пост-аудит M4-09 завершён. API smoke на изолированном проекте подтвердил v5 companion Э4, привязку статьи и удаление только синтетического проекта. Архив и хэш записаны в `docs/m4-10-post-m4-09-audit.md`; пользовательские данные не включались. Коммит и push не выполнялись по прямому указанию владельца.

M4-11 → M4-12 `0.23.0-m4-12`: исправлены маршруты и табличные точки v5,
геометрия экрана/витой пары и обновление терминалов ранее размещённого компонента.
Добавлены временное открепление привязки и явное перестроение. Клиент 747/747,
сервер 694/694; TypeScript, production build и API-smoke итогового пакета прошли.
ZIP без пользовательских данных: `artifacts/m4-12/TECHMAP-GRAPHER-win-x64.zip`.
Полный аудит, изменённые файлы, ограничения и SHA-256 —
`docs/m4-12-post-m4-11-audit.md`. Следующий новый этап — M4-13. Commit/push не делались.


M4-18 завершён: `0.28.0-m4-18`, редактируемое назначение контакта в Э4,
видимость серии при размещении, скрытие у экземпляра и у всех компонентов
текущей схемы. Клиент 789/789, сервер 718/718, TypeScript/build, API с перезапуском
и браузер прошли. ZIP без пользовательских данных — `artifacts/m4-18/TECHMAP-GRAPHER-win-x64.zip`.
Подробности и изменённые файлы: [m4-18-contact-purpose.md](m4-18-contact-purpose.md).
Следующая новая карточка продолжает индекс M4-19. Push остаётся заблокирован
ранее полученным отказом автоматической проверки точного адреса GitHub.
