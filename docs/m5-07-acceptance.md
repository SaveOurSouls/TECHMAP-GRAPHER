# M5-01–07 — передача на приёмку

Дата: 26.09.2026. Реализация M5-01–07 находится в коммите
`9aa33c296eefc9466695597e05f925f483cf845b`. План и фактический контракт:
[`m5-route-plan.md`](m5-route-plan.md), [`m5-implementation.md`](m5-implementation.md).

Переносимый Windows x64 ZIP приложения:
`artifacts/m5-07/TECHMAP-GRAPHER-0.63.0-m5-07-win-x64.zip`.
Размер: 55 143 461 байт. SHA-256:
`4AB9A04FD9494A945624504AC16AACA31A847591E330540E609276C6E8948E83`.
Архив локальный, `artifacts/` не включается в Git.

Проверки пакета: клиентские 1432/1432, TypeScript и production build прошли;
Release self-contained publish прошёл. Манифест пакета проверил 209 файлов по
размеру и SHA-256. ZIP распакован, `Techmap.Server.exe --verify-package` прошёл;
семь portable-режимов запуска, включая путь с кириллицей и пробелами, прошли.
В архиве есть версия, исполняемый файл, манифест, SBOM и пример справочника;
локальных пользовательских данных, `.git`, `node_modules` и тестовых логов нет.

Профильные проверки M5 и переноса ранее прошли: 113 .NET-тестов. Повтор полного
.NET-набора в ограниченной среде завис после запуска Web-тестов и был остановлен;
в предыдущем полном прогоне отдельно наблюдались нестабильный лимит времени поиска
справочника и зависимость теста старта от старого артефакта пакета. Эти результаты
не объявляются успешным полным прогоном. Сквозная ручная приёмка маршрута в UI
остаётся за владельцем.
