# М4-66–69: геометрия и визуальный стиль по референсам

Поручение 22.09.2026: С1 — светлый интерфейс с тёмной шапкой, бирюзовыми акцентами и компактными таблицами; С2–С3 — несовпадение рамки, таблицы и контактов, заливка рисунков; С4 — радиусные переходы; С5 — физические каналы и ветвления.

- М4-66: единые ширины колонок модели, Canvas и HTML; правильное направление связи seriesRowId → logicalContactId для заливки, разрешение цвета по logical ID без обязательного row ID. Новые контактные фигуры получают fillFromWire=true, существующие настройки не меняются. Клиент 897/897, TypeScript проверен. В существующем генераторном тесте добавлен сквозной путь через реальный экземпляр и проектный снимок.
- М4-67: визуальный стиль редактора и документных таблиц.
- М4-68: радиусные переходы прямых участков, физические каналы и узлы. Реализован общий путь Canvas с касательными дугами, редактируемыми исходными точками и визуальными лучами узлов; тесты покрывают прямой/обратный/короткий маршрут.
- М4-69: API, браузер, сборки и ZIP. Проверены 899 клиентских, 718 серверных, 54 доменных и 3 архитектурных теста; package verify, generator/channels API smoke и браузер на синтетическом проекте.

Все четыре этапа завершены. Следующий свободный индекс — М4-70.

## Приёмка М4-69

- Браузер, синтетический GENERATOR-HARNESS: выбранный HTML-компонент совпадает с Canvas и жёлтой рамкой; ввод длинной строки «Цепь» расширяет таблицу, а провода остаются на границах соответствующих строк. Вспомогательные контактные фигуры отображают красный и зелёный цвета проводов.
- Браузер, синтетический CHANNELS-TEST: заполненные каналы, цветные жилы, Т-ветвление и два выхода разъёма; перенос точки гиба с (300, 500) на (361, 489) перестраивает касательную дугу, Ctrl+Z возвращает исходную точку.
- На окончательном пакете проверены спецификация, двухуровневая таблица соединений и карта резки: компактные строки, бирюзовое выделение связанной позиции, доступные поля и прежние действия. Таблицы открываются нижними закреплёнными окнами; способы закрепления и изменение размера сохранены. Каталог можно свернуть.
- TypeScript и 899 клиентских тестов проходят; Release-сборка сервера, 718 Web + 54 Domain + 3 Architecture теста проходят. Клиент пересобран после финальных CSS-правок.
- Повторные `test-drawing-generator.mjs` и `test-harness-channels.mjs` на окончательном пакете проходят: закреплённая библиотечная привязка, рисунки всех трёх разделов, дополнения, поворот, сохранённые снимки, ответвления, выходы, идентичность проводов, спецификация и повторный запуск сервера.
- `verify-package.ps1` подтвердил манифест. ZIP собран из чистой поставки без пользовательских проектов и тестовых данных; проверки запускались с отдельными каталогами `artifacts/*-smoke/test-*`.

## Поставка и границы

Версия: `0.45.2-m4-69`, схема данных 20.
Архив: `artifacts/m4-69/TECHMAP-GRAPHER-win-x64.zip` (53,52 MiB).
SHA-256: `D9706B79FD9DE088C018F3DB3E29054FD04701CC20D14C61FAD8DE579B83DD7D`.

Радиус переходов пока фиксирован: 24 условные единицы чертежа, с уменьшением на коротких участках. Это визуальная геометрия, не заданный производственный радиус в мм; расчёт измеренных длин и электрическая топология не изменены. Ранее отключённая заливка `fillFromWire` сохраняется; для новой контактной фигуры она включена по умолчанию. Сборка Vite выдаёт прежние предупреждения о размере JS и browser-external модулях зависимости HEIC; ошибок сборки нет.
